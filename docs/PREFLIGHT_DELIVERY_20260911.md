# T0–T5 工程交付（2026-09-11）

已执行修订后的 T0、T1 及原计划 T2–T5；未启动 T6/T7、未训练、未恢复旧模型。事实基线为 `3e5694c`，全部新数字均可回到本次 JSON；原先无产物支撑的回报/动作审计数字继续排除。

## 核心结论

四个测试时域的持续失败集合为 **{262005, 262006}**。保持原 MPC、约束和动力学，仅改上层脚本接近策略，已救回 **262005**；**262006 在本次有限网格中仍未救回**。因此既不能宣称两种子救援目标已完成，也不能宣称 262006 物理不可达。反馈预检有描述性信息，但不能代替尚缺的第二个工况救援证据或学习消融。

| 阶段 | 实际交付与结论 | 证据目录/报告 |
|---|---|---|
| T0 | profiler 诊断开关；h20/262000 的 300×6 指令逐位相同；初始全量 205 passed | `logs/t0_amendment_20260910/` |
| T1 | h20/h30/h35/h50 完成率 8/12、9/12、9/12、10/12；补跑 24 局及 4 局指标复测 | `docs/P0_HORIZON_PERSISTENT_FAILURES.md` |
| T2 | 144 个表格格点，98 个独立实跑产物，其余为明确标注的等价复用；时机对照救回 262005，半径额外救回 0 个种子 | `docs/P1_A_COMMIT_TIME_AND_HOLD_RADIUS.md` |
| T3 | 仅 262006，30 格速度诊断均失败；延迟切入格在切入前已失败，不能说这些格实际测试了推进速度 | `docs/P1_B_APPROACH_RATE_SCAN.md` |
| T4 | 仅 262006，24 个非零角度格及 2 个零角度对照均失败 | `docs/P1_C_LATERAL_SCAN.md` |
| T5 | 2 个成功、4 个失败回放；仅增加 info 反馈，屏蔽回退步残留 slack；整体分布/预警有信号，持续恶化趋势未过门槛 | `docs/P2_FEEDBACK_SEPARABILITY.md` |

## 距离通道的实际价值与边界

“新增救援种子为 0”不等于半径完全无价值。262005/h20/60 s：起始半径保持完成于 293.8 s、354.83 N·s；14 m 保持完成于 271.2 s、281.74 N·s。同一时刻/时域/种子下，两者均完成，燃料可作配对比较。262005/h35/30 s：起始半径保持失败，12 m 保持成功（91.1 s、316.76 N·s）；它扩展了该种子的可行格，但该种子已能被其他切入时刻救回。这些都是事后逐种子扫描的动作空间上限，不是可学习性证明。

## 唯一新算力口径

串行单进程、seed 262000、前 300 步、关闭事后诊断、包含首次求解，`controller.command` 的 p95 分别为 **47.53、68.21、77.44、113.59 ms**，对应 h20/h30/h35/h50。超 100 ms 比例分别为 0.33%、0.33%、0.67%、89.67%。原始 mean/max/逐格配置均在 T1 报告和 compute JSON。前缀采样不构成全程硬实时保证；并行扫描耗时从未用于实时结论。

## 反馈证据

有效求解 slack 的固定预警规则在本组中检出 2/4 次失败、成功局零误报，检出样本中位提前 11.15 s；回退比例规则检出 4/4，但成功局误报 1/2。全部规则 OR 会对两个成功局都误报，因此不能把多个信号粗暴合并成已验证安全报警器。趋势统计保留全部失败样本为分母，不能删掉全回退导致 slack 缺失的案例后夸大趋势。只有两个种子、无留出集，动作/相位/时长仍可能混杂；只能支持后续受控消融候选。

## 工程验证与冻结边界

最终全量回归：**211 passed in 121.49s (0:02:01)**。新增测试覆盖惯性保持、对照等价、径向推进、横向零角度对照与径向不变性，以及残留 slack 屏蔽。6 个反馈回放均保持原结果和终止时间，力冲量差在 1e-7 N·s 内。对照 `3e5694c`，`controllers/`、`dynamics/`、任务/奖励/环境配置文件和 `train/` 没有改动；`env/hybrid_env.py` 仅增加 info 统计，观测和默认参数不变。原 V4 模型和日志未覆盖，未加载检查点训练。

所有阶段都有独立提交及 manifest。T2 中途进程不再存在时，保留了 98 个已完成产物；剩余 6 格根据更早切入前已终止的同一保持轨迹做精确复用，来源逐格记录。T3/T4/T5 使用已经过等价验证的事后诊断关闭与目标按需传播开关加速；这不改变 RK45 参数或任务。

**停在授权边界：T6 接口冻结和 T7 训练均未执行。** 下一次上层裁决必须面对仍未救回的 262006，不能将本次部分成功写成整个耦合方案已成立。
