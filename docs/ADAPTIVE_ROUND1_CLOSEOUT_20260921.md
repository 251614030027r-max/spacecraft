# 自适应耦合第一轮：收尾与根因裁决

*2026-09-21。分支 `claude/sac-mpc-coupling-design-ns7g6i`。*

本文收束 `adp_rf_262410/411/412` 这一轮（从零训练 60,000 decision steps ×3，
reward 单位修正后），给出根因裁决，并交出下一轮设计所需的全部实测前提。
协议见 `COUPLING_DEFECT_DIAGNOSIS_ORDER_20260921.md`，逐步审计见
`COUPLING_DEFECT_DIAGNOSIS_LOG_20260921.md`。

---

## 1. 一句话

> **棘轮把整个任务决策压缩到第 0 步，使其余约 40 次决策全部惰性；策略第 0 步的
> commit 残差中位数是 +0.97，正好落在占通道 50% 的那个死点上。暂存本身有真实价值
> ——脚本化暂存在 Pure MPC 全败的 12 个种子上救回了 4 个，学习层一个都没拿到。**

根因 = **R5（决策被棘轮压缩到第 0 步）+ R2（暂存分支在探索中不可达）**。
R1（门语义）是次要成因，约 25%。**R3（表达不了）与 R4（任务没决策）已证伪。**

## 2. 这一轮建立了什么，没建立什么

**建立了：**

- reward 单位修正是对的。三种子训练期完成回报稳定 ≈ +15.5，hover ≈ +4，方向未再反转
  （`logs/adp_rf_*/train.monitor.csv`）。+15.5 落在"擦边完成"档（沙箱预测 +16.7）而非
  "干净完成"档（+26），预示裕度偏薄——**这个预测后来被证实**，主行全局最薄真值归一化
  裕度 +0.0091。
- 训练数值健康：无 NaN/Inf，critic loss 0.043–0.058，actor loss 连续有限。
- **接口存在一个可复现的结构缺陷**，已由三个 strict xfail 测试钉住
  （`tests/test_coupling_diagnostics.py`，T-A/T-B/T-C）。

**没建立（不得写进论文或汇报）：**

- ❌ **"耦合方法无效"**。它没生效：有效介入 0.114%（262410）/ 0.484%（262411），
  D1 的 8 种子子块上恰好为 0。有效性**未知**。
- ❌ **"已验证基线保持"**。retained 36 / rescued 0 / destroyed 0 是**靠不作为**得到的；
  而"残差 0 逐位等于 Pure MPC"本来就有单测钉着。**那是恒等式，不是实验结果。**

## 3. 根因裁决

| 代号 | 根因 | 裁决 | 证据 |
|---|---|---|---|
| **R1** | 门的回退带不可逆副作用 | **次要，约 25%** | D1：gate fallback 6/24、policy 18/24。独立地，48 种子行第 0 步门回退 16/48（262410）+ 8/48（262411）= 24/96 = 25%。**两组独立样本给出同一个 25/75 分割** |
| **R2** | 暂存分支在探索中近乎不可达 | **成立（指示性）** | D0 三模型随机滚动 latch 中位数 0、12/12、有效参考改变 0/973；D1 确定性均值同样第 0 步 commit。定性待 D0b |
| **R3** | 动作几何表达不了暂存 | **证伪** | `r = -0.265` → blend 0.735 → 7.42 m，正是离线可行性证书的暂存半径；脚本臂实测单回合有效介入 26.8% |
| **R4** | 任务里没有那个决策 | **证伪** | **D4：`n=5` 救回 262001/262039，`n=15` 救回 262011/262035，四个全部零违约完成** |
| **R5** | **决策被棘轮压缩到第 0 步，而策略的暂存意图出现在之后** | **成立，主因** | 见第 4、5 节 |

## 4. 机理：接口的几何

三处独立合理的设计合成一个吸收态（`env/hybrid_env.py`）：

1. `a_commit = clip(1 + 2r)`，**所有 `r ≥ 0` 映射到同一点**；
2. `blend = max(历史最大, 本次)`，`blend` 到 1 后**直接 return 捕获位姿**，
   负残差被 max 吃掉、`a_radius` 被整条绕过；
3. 门的回退动作是零残差，即 `r = 0` → `blend = 1` → 锁存。

实测（`monotone_commit=False` 隔离映射本身）：

- **21 个等距 commit 残差 → 只有 11 个不同参考点**；`r ∈ {0.0, 0.1, …, 1.0}` 这 11 个
  全部落在捕获位姿上，逐位等于 Pure MPC。
- **一次门回退之后，2D 动作网格 15 个点 → 1 个参考点。整个动作空间 100% 惰性**，不是一半。

`blend` 直接决定参考半径（20 m 冻结起点 → 3 m 捕获位姿）：

| 第 0 步残差 | blend | 参考半径 |
|---:|---:|---:|
| −1.00 | 0.000 | 20.00 m（原地不动，不接近） |
| −0.40 | 0.600 | 9.80 m |
| **−0.265** | **0.735** | **7.42 m ← 离线可行性证书的暂存点** |
| −0.10 | 0.900 | 4.70 m |
| **≥ 0（含策略实测中位 +0.97）** | **1.000** | **3.00 m，Pure MPC** |

所以物理上有用的那一段是 `r ∈ [−0.3, −0.1]`，**占整个通道宽度的 10%，紧邻一个占 50%
的死点**。

**策略的意图是有的，只是时间错了**（从 48 种子行的 `policy_actions_raw` 直接提取）：

| | 262410 | 262411 |
|---|---|---|
| 第 0 步 raw commit 残差中位数 | **+0.9714** | **+0.9546** |
| 第 0 步 raw < 0（想暂存） | 1/48 | 1/48 |
| **整局中曾出现 raw < 0** | **47/48** | **48/48** |
| **首次 raw < 0 的决策序号（中位）** | **12** | **3** |

例（种子 262000）：第 0、1 步被门回退成零残差 → 锁存；第 2 步策略要求
`[−0.937, +0.082]`（接近完整暂存）——完全无效。

**为什么会学成这样**：从 15–28 m 出发，`blend` 同时承载"距离接近"和"对旋转位姿的承诺"
两件事，而 shaping 奖励接近。策略无法表达"再靠近一点，但先别承诺那个旋转位姿"，
只能把 blend 推到 1。加上棘轮使第 0 步不可逆，**一个决策决定全局、其余约 40 个惰性**
——这种信用分配结构本身就极难学。

## 4b. P0：第 0 步的意图，三模型 × 48 种子

`experiments/probe_policy_intent.py`。第 0 步棘轮还咬不住，所以参考异于固定设定点的
充要条件就是 commit 残差为负——整个决定性时刻退化成一个数的符号，一次 reset 加一次前向
传播即可读出，**不做任何滚动**。算法假设的那条代数已改为由环境测量交叉验证，
**144/144 全部通过**。

| 模型 | 第 0 步残差 中位 | 残差范围 | 想暂存 | 门会放行 | critic 优势 中位 / 范围 | **暂存存活到执行** |
|---|---:|---|---:|---:|---|---:|
| 262410 | +0.9714 | [−0.3190, +0.9998] | 1/48 | 32/48 | +0.0791 / [−0.221, +1.744] | **1/48** |
| 262411 | +0.9546 | [−0.1139, +0.9999] | 1/48 | 40/48 | +0.1149 / [−0.295, +0.530] | **1/48** |
| 262412 | +0.9902 | **[+0.3796, +0.9997]** | **0/48** | 39/48 | +0.1246 / [−0.124, +0.595] | **0/48** |

三件事：

1. **三个模型的第 0 步动作全部饱和在通道的死半边。262412 在 48 个种子上残差最小值是
   +0.3796——它在任何状态下都没有能力在第 0 步暂存。**
2. **门在多数状态下是放行的**（32/40/39 of 48），回退 16/8/9，均值 23%，与 D1 的
   6/24 = 25% 一致。三模型 144 个 episode 里，**暂存存活到执行的只有 2 个**。
3. **critic 优势是个跨零的小数**（中位 +0.08…+0.12）。所以门在约三分之一的状态里，
   决策取决于两个 Q 值的微小差，其中一个还是离策略分布的动作。这正是仓库 2026-09-16
   自标的 `Q(s, a_nom)` 隐患，**现在有数字了**。

### 这补上了 R2 的机理，不只是"没探索到"

`r ≥ 0` 那半边不是"没用"，是**梯度恒为零的平台**：在那里 commit 通道对参考的偏导
处处为 0，策略**感觉不到这个通道存在**。所以它是一个自我强化的陷阱——任何游荡进
`r ≥ 0` 的策略，在该通道上再也得不到推它出来的梯度。

这解释了为什么三个独立种子全部收敛到同一处，也说明 **R2 不必依赖"训练早期未探索"这个
假设**：即使早期探索到过暂存，一旦进入死半边就出不来。D0b（5k/10k checkpoint）仍有价值
——它区分"一开始就在里面"和"漂进去的"，但两者的修法都是 F2/F3。

## 5. 决定性证据：救回是"不相交"的

`--control staged_residual`（不经策略、不经门，走真实残差接口），只跑 Pure MPC 全败的
12 个种子（`nominal_pairing_two_model_interim.json` 的 `both_failed`）：

| 暂存时长 | 完成 | 救回种子 | 零违约 | 最薄真值归一化裕度 |
|---|---:|---|---|---|
| 10 s（n=5） | **2/12** | 262001, 262039 | 是 | 0.0198 / 0.0189 |
| 30 s（n=15） | **2/12** | 262011, 262035 | 是 | 0.0199 / 0.0196 |

**两个固定时长救回的是不相交的种子集。**

- 任何**单一固定**暂存时长，最多 **2/12**；
- 按状态**选**时长，**4/12**。

这个 2 倍差距就是"同一策略随状态改变行为"的**实测价值**，而且是在主线任务自己的产物上
测出来的，不是从 T12 继承的论证。12 个难例中 8–9 个死于超时，而暂存花的正是时间——
救回的是几何收益压过时钟代价的那些，这是自洽的物理解释。

## 6. 上界，以及它的边界

| 方案 | 48 种子块完成 |
|---|---|
| Pure MPC | 36/48 |
| + 固定 10 s 或 30 s（仅施加于难例） | 38/48 |
| **+ 按状态选时长（预言机上界）** | **≥ 40/48** |

**必须同时说清**：40/48 是**预言机上界**，要求门控完全正确；且暂存施加到 Pure MPC 成功的
36 个种子上**大概率会毁掉一部分**（8–9/12 的失败是超时，暂存花时间）。破坏效应由 **D4c**
测，未出结果前 40/48 **不是一行结果，只是上界**。T12 那个继承来的"诚实上界 38/48"看起来
是低估。

## 7. 待定测量

| | 回答什么 | 状态 |
|---|---|---|
| D4 `n=30` | 60 s 暂存是否再扩大救回并集 | 队列中 |
| **D0b**（5k/10k checkpoint 随机滚动） | **「从一开始就饱和在 commit」还是「探索过暂存又放弃」——两者修法不同（改接口 vs 改奖励）** | 队列中 |
| **D4c**（暂存施加到 36 个成功种子） | **破坏效应；没它第 6 节的上界站不住** | 队列中 |
| D3（3 翻滚率 × 两策略） | 跨工况代价结构差异 | 队列中 |

**D2（关门 48 种子）已无必要**：D1 已测出有效介入恰好为 0，去掉门不会改变这一点，
因为 75% 的锁存是策略自己造成的。

## 8. 修复优先级

| | 优先级 | 依据 |
|---|---|---|
| **F3 棘轮处置** | **第一** | 第 0 步不可逆使其余决策惰性，摧毁信用分配。**坑：它"8/8→0/8"的正当性是在旧任务（15–20 m、无暂存通道）上量的，必须在当前任务重测，不能继承；也不可直接删** |
| **F2 重参数化** | **第一** | 有用带 10%、紧邻 50% 死点；且 `blend` 混淆了"距离接近"与"位姿承诺"两个语义，必须拆开。判据用 T-A |
| **F1 门语义分离** | 第二 | 原则问题（安全回退不得写不可逆模式状态），但只占 25%。判据用 T-B |
| **F4 门的对象一致** | 第二 | 门应对"映射后参考/模式转移"去重，报告 effective-reference deviation |

## 9. 下一轮设计的入口

这一轮把机制创新的**动机从审美变成了实测**：

- 一个纯启发式的 critic 阈值门，在这套接口上不但没帮忙，还因为回退动作带不可逆副作用
  贡献了 25% 的锁存。→ **"把接口做成可证的、把安全回退与模式转移分开"有了实测依据。**
- 决策被压缩到单步、其余惰性，是信用分配问题。→ **"用 MPC 已经算好但被丢弃的前瞻做
  critic 的多步目标"针对的正是这个病**，且额外算力为零。
- **注意边界**：MPC 时域 3.5 s、决策周期 2 s、翻滚周期 152.4 s。下层前瞻看不到
  "同步 vs 暂存"这个尺度，所以它改善短时程信用分配，**供不出长时程决策信号**。

**不建议在接口修好前重训。** 接口不动，重训只会再烧两天得到同一个结果。

## 10. Artifact 索引

| 内容 | 路径 |
|---|---|
| 三个模型 / 训练曲线 / manifest | `logs/adp_rf_26241{0,1,2}/` |
| 5k / 10k checkpoint | `logs/adp_rf_26241{0,1,2}/checkpoints/` |
| Pure MPC 与两模型 nominal 行 | `eval/adp/pure_mpc_nominal.json`、`proposed_nominal_adp_rf_26241{0,1}.json` |
| 成对四象限 / 主表 | `eval/adp/nominal_pairing_two_model_interim.json`、`nominal_main_table_two_model_interim.json` |
| D0 / D1 | `eval/adp/d0_stochastic_*.json`、`d1_gated_instrumented_*.json` |
| P0 第 0 步意图（已发布） | `eval/adp/p0_intent_26241{0,1,2}.json` |
| D4 / D4c / D0b / D3（本轮新增） | `eval/adp/d4_staged_n*.json`、`d4c_retained_n*.json`、`d0b_stochastic_*.json`、`eval/cal2/*.json` |
| 缺陷的可执行证据 | `tests/test_coupling_diagnostics.py`（T-A/T-B/T-C，strict xfail） |

> `eval/` 产物被 `.gitignore` 排除；需要发布的按下层"publish evidence"的做法 force-add。
> 另：`experiments/probe_policy_intent.py` 仍未跟踪，作者非本窗口，收录与否待定。
