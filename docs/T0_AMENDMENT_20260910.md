# T0 修订令：以 3e5694c 为唯一事实基线（2026-09-10）

> **本文取代执行计划 rev.2 的 T0 全节，并修订 T1。其余阶段（T2–T5）不变，T6/T7 仍未授权。**
> 触发：下层 `T0_RECONCILIATION_BLOCKER_20260910.md` 的现场核实报告。

---

## 1. 裁定：下层核实正确，T0 原前提作废

下层报告的核实结论**成立**，上层已在独立环境复核同一 HEAD，结果一致：

- 本地与远端 `claude/sac-mpc-coupling-design-ns7g6i` 同为 `3e5694c`，`rev-list --left-right` 为 `0 0`
- 全仓库不存在 h30/h35 的任何数据（`logs/` 下按时域字段和文件名两种方式检索均为空）
- `experiments/audit_hybrid_return.py`、`experiments/audit_solution_actions.py` 不存在
- `docs/HORIZON_SWEEP_AND_WITHDRAWAL_20260910.md` 不存在

**"本地领先 36 个提交"这个前提是错误的，作废。** 下层按计划要求停下来报告，处理正确；
没有创建虚假同步 commit、没有补写同名文件冒充历史资产，这两条做法也正确，予以确认。

**从此刻起，`3e5694c` 是本项目唯一的事实基线。** 任何在它之外声称存在的产物，
在被重新产出之前都不算数。

---

## 2. 由此产生的后果：一批数字失去支撑，不得引用

以下数字曾在上层交接材料中流传，但**在 `3e5694c` 中没有任何产物支撑**，
因此一律标记为**未复现，不得写入论文、不得作为任何决策依据**：

| 失效数字 | 原声称内容 |
|---|---|
| h30 / h35 时域行 | 9/12、88.4 s、167.2 N·s |
| h35 串行 p95 0.82× | 关闭诊断后的实时倍率 |
| "h50 关掉诊断后是 1.18× 而非 2.22×" | 算力撤回结论 |
| 回报审计 | 完成 +14.11 / 零动作 +1.20 / 随机 −17.8 / 训练后 −24.0 |
| 动作空间审计 | 成功解径向通道 a₀ = −0.76 ~ −0.81，24~28% 打边界 |

**处理方式**：

- **需要的**（h30/h35 行、算力口径）→ 由修订后的 T1 重新产出。
- **不需要重做的**（回报审计、动作审计）→ 它们支撑的是"零动作等于不接近"这一**设计动机**。
  该动机已被 V3/V4 两轮 0 完成的训练结果独立支持，不必为它再花一轮实验。
  **但论文中不得引用这两组数字。**

### 仍然有效的资产（上层已独立复核）

`logs/upper_na_f1/` 的 12 种子 × h20/h50 逐局 JSON **真实存在且可读**。上层直接读出的结果：

| 时域 | 失败种子 |
|---|---|
| h20 | 262001, 262005, 262006, 262011 |
| h50 | 262005, 262006 |
| **两时域交集** | **262005, 262006** |

终止特征（h20，取自同一批 JSON）：

| 种子 | 终止时刻 | 终止距离 | FOV 峰值 | 平均推力 | 非法穿越 |
|---|---|---|---|---|---|
| 262005 | 96.0 s | 10.61 m | **49.83°** | 4.03 N | 5 |
| 262006 | 34.9 s | 17.22 m | **49.92°** | 1.27 N | 1 |

FOV 上限 50°，**两个都是撞视场约束死的**。这条可以使用。

---

## 3. 好消息：T1–T5 不依赖任何缺失资产

逐条核对后，缺失的三样东西对前置验证都**不构成实质阻塞**：

| 缺失项 | 对 T1–T5 的影响 | 处理 |
|---|---|---|
| h30/h35 数据 | T1 需要 | **自己跑**，24 局，见 T1' |
| `--no-diagnostics` 参数 | T1 算力复测需要 | **不是缺失功能**：`MPCConfig.runtime_diagnostics` 字段已存在（`controllers/mpc/config.py`），`online_endpoint_mpc_config` 已在用 `False`。profiler 只是没暴露成 CLI 参数，约 4 行改动，见 T0' |
| 两个 audit 脚本 | **T1–T5 完全不用** | 不补建 |

所以**解除阻塞的成本很低**，不需要等待任何外部投喂。

---

## 4. T0'（取代原 T0）：补建，而不是同步

**没有可同步的提交，不要创建同步 commit。** 改做两件小事：

### T0'-a：给 profiler 加诊断开关

在 `experiments/profile_precapture_mpc.py` 加 `--no-diagnostics` 标志，
传进已有的 `MPCConfig.runtime_diagnostics` 字段（该脚本已用 `replace(...)` 构造 config，
在参数表里多加一项即可）。

**验收**：同一 seed、同一 horizon，开/关两次运行的**指令逐位相同**（诊断只做事后统计，
不参与 QP 与选定指令）；只有耗时不同。这条必须实测确认，不能默认。

### T0'-b：修正两处口径声明（不删原文，加声明）

- `docs/PRECAPTURE_ENTRY_WINDOW_GAP_WITH_F1.md` 顶部：其 h20 p95 0.94×、h50 p95 2.22×
  是**开着诊断**测的，**将被 T1' 的部署等价复测取代**。
  （注意：此前上层材料称这两个数"已被撤回并修正为 1.18×"——**那个修正本身也没有产物，一并作废**。
  在 T1' 出结果之前，这里没有任何已知正确的算力数字。）
- `docs/PRECAPTURE_TASK_SPEC.md:106` 的 0.82× 旁：这是 **V0.2 / h20 / 固定设定点 / seed 262000**
  的历史调参证据，与任何 h35 实时倍率**无关，数字巧合**，不得并列引用。

### T0'-c：核实全量回归

跑 `python -B -m pytest -q`，**记录实际数字**。此前流传的 "207 passed" 未经核实，以本轮实测为准。

**产出**：一条 commit，`chore: expose profiler diagnostics switch and reconcile compute claims`。

---

## 5. T1'（修订 T1）：数据要自己跑，且顺带产出 Pure MPC 行

### 变化点

1. **h30、h35 必须自己跑**，不存在可复用数据。设置必须与 `logs/upper_na_f1/na_f1_*` 完全一致：
   `precapture_planning`、固定设定点、`analytic_local`、真值状态、`perception=None`、每格一局、12 种子。
2. **每局同时记录完成/时间/力冲量/最差真值裕度**，不只记完成与否。
   原因：主表的 Pure MPC 行同样没有可信数字（见 §2），**这一批运行就是它的来源**，
   顺手记下来比以后重跑一遍便宜得多。
3. **算力复测用 T0'-a 加好的 `--no-diagnostics`**，串行单进程，测 h20/h30/h35/h50 的
   mean / p95 / max 与超预算步数占比。**本轮结果是此后唯一有效的算力口径。**

### 判据（不变）

- **|F| ≥ 2** → 继续 T2。（按现有两时域数据 |F| = 2，恰好过线；补上 h30/h35 后只会变少不会变多。）
- **|F| = 0** → **停止并报告。** "加长时域解决不了"这个立论不成立。
- **|F| = 1** → 报告后再定。

### 措辞纪律（不变）

只能写"**在所测试的预测时域范围内，存在对时域扩展持续不敏感的困难工况**"，
不得写成"任何时域都失败"。

**产出**：`docs/P0_HORIZON_PERSISTENT_FAILURES.md` + 逐局 JSON + 算力 JSON。

---

## 6. 其余不变

T2、T3、T4、T5 按 rev.2 执行，包括：

- T2 必须分两层（`commit_at` 只扫时刻作对照 → 再加保持半径看增量），归因不得混淆
- T2–T4 报完整网格，不只报"救没救"；每格附燃料
- T3 的"移动航点表达速度"是**诊断手段**，不是接口结论
- T5 反馈量只用真便宜的三类；`predicted_minimum_margin` 仅离线诊断用；
  先查 `maximum_slack` 在回退步上是否为残留值；可分性分三层看，不一票否决
- **T5 完成后无论结论如何都要停下来等 T6 授权**

红线不变。

---

## 7. 这次事件的一条纪律

本轮暴露的问题是：**上层材料里出现了没有产物支撑的数字，并被当作既成事实往下传。**

此后一律执行：**任何进入决策或论文的数字，必须能在 `3e5694c` 之后的仓库里指到具体产物文件。**
指不到的，一律标注"未复现"，不得引用。下层若在计划中读到指不到产物的数字，
按本次同样的方式停下来报告即可。
