# Phase 2 纯 SAC 阶段复盘与下一步方向审查说明

**研究对象：** 高保真六自由度快速翻滚非合作目标预捕获任务  
**用途：** 与工程审查包一并提交导师，说明当前问题链、已完成排查、暂停原因及待裁决方向  
**状态：** Pure SAC S1 暂停；不进入 S2；等待架构级方向审查  
**日期：** 2026-08-17

## 0. 一页结论

经过多轮严格单因素实验后，Pure SAC 已经能够分别学习“向 Gate 接近”和“降低接近速度”等局部技能，但始终没有在独立种子上形成稳定、可泛化的完整 S1 acquisition 策略。问题已不再像是某一个超参数、坐标系或简单 reward 错误，而更像是当前“端到端 SAC 直接输出六维力/力矩”的任务表述对长期六自由度联合控制、信用分配与策略稳定性的要求过高。继续以 reward/entropy/tau/sampler 等方式死磕，投入产出比已经很低。

这并不等于 SAC–MPC 主线失效。现有实验更清楚地暴露了 model-free 直接低层控制的弱点：SAC 能学到任务方向和局部行为，但很难稳定完成位置、速度、姿态、角速度的长期协调；MPC 的模型预测、约束处理和闭环执行能力正好对应这些缺口。真正需要导师裁决的是：后续是否仍要求 Pure SAC 作为“成熟低层控制器”，还是把 SAC 的角色调整为高层参考、任务先验或 MPC 辅助信息源。

当前建议：暂停所有新的 Pure SAC 长训练，不继续调 reward、observation、tau、entropy、sampler 或 curriculum；保留现有最佳 checkpoint 和全部失败证据，先做一次架构级审查。Phase II 尚未正式展开，如果不在此处止损，后续调试时间风险很高。

## 1. 研究任务与原始主线

研究对象是快速翻滚非合作目标下的高保真六自由度近距离交会/预捕获控制。环境保留 SE(3) 姿轨统一、绝对刚体运动、中心引力、J2、重力梯度、RK45 truth propagation、0.1 s 控制周期以及六维力/力矩执行。论文主线原本希望形成“Pure SAC → constrained MPC → SAC–MPC”的对照链，重点比较任务收敛、约束满足、控制消耗和在线计算。

两阶段任务定义中，Phase I 是外围 acquisition，Phase II 才进入 35° corridor、50° FOV、terminal velocity constraints 等真正受约束的终端预捕获过程。当前工程中的 MPC-only 基础路径已经存在；但在新的两阶段任务语义下，Pure SAC 一直没有形成可作为 S2 初始化的合格 S1 模型。早期任务边界文档中“Pure SAC 基线已完成”的假设已经被后续 S1 实验推翻，应视为历史前提而非当前事实。

## 2. 为什么现在必须暂停，而不是继续“再调一轮”

S1 已经连续排除了 reward/termination 捷径、curriculum、fixed entropy、local progress、初始几何过宽、状态—动作坐标错位以及 moving Bellman target 过快等多个问题。每个修改都带来某个局部改善，却始终不能组合成稳定策略。更重要的是，S1-v2 已主动降难，v3 又显式提供 velocity tracking error，但独立 20 seeds 仍然 Gate 0。Phase II 尚未开始，继续把大量时间消耗在 Pure SAC S1 上，项目节奏风险已经很高。

## 3. 已完成的问题排查链

| 阶段 / 单因素 | 主要结果 | 当前结论 |
|---|---|---|
| reward/termination 语义修正 | PD 在 canonical truth 上 20/20 Gate；修正奖励捷径后 SAC 仍失败 | 物理可控性不是根因，reward 问题并非全部原因 |
| adaptive curriculum | 训练成功多但 full canonical eval 仍0，replay 被长失败 episode 主导 | 停止 curriculum |
| full canonical + fixed alpha | 250k Gate 0，后期数值/行为退化 | curriculum 不是唯一根因 |
| automatic entropy | Q/loss/饱和显著改善，Gate仍0 | 保留 auto entropy，但不足以解决任务 |
| local progress | 早期位置改善，但高速接近、姿态恶化且 final 遗忘 | 停止 reward patch |
| cone 55°→20° | 明显降几何难度仍失败 | sampler 过宽不是主因 |
| body-frame observation | 早期接近明显增强 | 坐标对齐有效，保留 |
| tau 0.005→0.001 | Q/loss/alpha 爆炸基本消除，但 Gate仍0、行为仍遗忘 | 数值稳定不是最终根因 |
| S1-v2 Acquisition | 400k 独立20 seeds：16/20靠近≥1 m，10/20进位置球，16/20 FOV合格，仅3/20低速，Gate0 | SAC 已学会空间 acquisition，瓶颈转为主动制动/联合控制 |
| v3 velocity-error observation | 200k fixed eval首次1/10 Gate；独立20 seeds 19/20低速、12/20靠近≥1m，但0/20进位置球，Gate0 | 速度技能有效，但与深度位置 acquisition 权衡；局部技能无法稳定组合 |

## 4. 当前核心诊断

当前 direct-wrench Pure SAC 的问题已经从“不会学”转变为“会学若干局部技能，但难以把它们稳定组合成一个长期、泛化的六自由度闭环策略”。actor 需要同时自行发现三维 guidance、body-force 映射、随机初速度抑制、Gate 前制动、姿态同步、角速度调节、到达后保持和失败恢复。这个 formulation 把大量本可由模型和控制结构显式提供的关系全部交给函数逼近和 bootstrap value learning。

继续在同一 formulation 内增加 reward 权重、额外状态字段或 curriculum，风险是把论文变成“为了让 SAC 收敛而不断补丁化工程”。

## 5. 对论文主线的含义

当前结果不支持继续把“Pure SAC 直接六维 wrench 控制必须先成为成熟控制器”作为第二阶段前置条件，但并不否定 SAC–MPC。更自然的研究动机是：学习策略提供任务方向、局部经验和快速决策，MPC 提供模型一致的长期协调、约束保证和稳定执行。

## 6. 建议导师裁决的三个方向

### A. 降低 RL 的控制抽象负担（当前更推荐）

让 SAC 输出局部相对速度参考、短时 pose/velocity reference、terminal target、MPC cost/weight hint 或其他任务先验，由 constrained MPC 生成六维力/力矩。当前实验已经证明 SAC 对“往哪里走”和“应该慢下来”是可学习的，真正困难的是长期组合和可靠执行。

### B. 保留 direct-wrench SAC 作为“有能力但不可靠”的弱基线

不再要求 Pure SAC 独立通过 S1 才允许后续工作。Hybrid 必须由 MPC 提供主要可行性/约束保证，不能依赖 actor 已经可靠。

### C. 若必须要求 Pure SAC 独立成功，则做结构级重构

如果导师明确要求 Pure SAC 必须稳定通过 S1，应重新设计控制层级或学习问题，而不是继续 reward/entropy/tau sweep，例如 hierarchical/goal-conditioned tracking、显式 reference tracking 或 guidance/stabilization 分层。这已经属于方法重构，应先评估论文贡献和工程成本。

## 7. 当前停止项

暂停新的 Pure SAC 长训练；暂停 reward、observation、tau、entropy、learning rate、train/update ratio、sampler、Gate、curriculum 等继续 sweep；不加载中间 checkpoint 碰运气续训；不在 Pure SAC S1 未稳定的情况下直接进入 S2。

## 8. 哈工大 TAES 文献使用边界

该文早期主要用于 SE(3) 航天器动力学建模与任务场景参考，相关动力学复现已完成。根据本项目实际复现过程，论文若干参数设置、训练细节和工程定义存在较多疑点或不合理之处，算法结果无法作为可靠可复现 benchmark。后续不再用其作为 SAC 训练步数、reward、超参数、成功阈值或收敛速度依据；如最终论文引用，仅作为相关工作/SE(3)+DRL 场景背景。

## 9. 建议导师直接回答的问题

1. Pure SAC 是否必须在 Phase I 独立稳定成功，还是允许作为有局部能力但可靠性不足的 baseline？
2. 是否同意把 SAC 从直接六维力/力矩输出调整为局部参考/任务先验，由 MPC 负责受约束执行？
3. 如果调整 RL 角色，主线更偏“学习型 guidance/reference + MPC”，还是“actor action prior + MPC refinement”？
4. Phase I 是否仍必须独立 RL pretrain，还是可由 model-based controller 承担 acquisition，把学习重点放到 Phase II？
5. 论文目标更强调任务型工程贡献，还是必须增加更强算法创新？

## 10. 当前事实快照

- 高保真 truth / controllability：已验证，物理任务可控。
- Pure SAC S1：未合格，独立 deterministic Gate 仍为0。
- S1-v2 400k：独立20 seeds 中16/20靠近≥1 m、10/20进入位置球、16/20 FOV≤45°、仅3/20最近点速度≤0.30 m/s。
- v3 200k：独立20 seeds 中12/20靠近≥1 m、19/20最近点速度≤0.30 m/s、20/20 FOV≤45°，但0/20进入位置球，Gate 0。
- tau=0.001：明显抑制 Q/critic/alpha 数值爆炸，但没有解决最终行为遗忘。
- Phase II：尚未正式进入。

**最终建议：** 当前暂停 Pure SAC 是合理止损，不应理解为项目失败。下一步必须先确定 SAC 在 SAC–MPC 中究竟承担低层执行、任务参考、动作先验还是代价/终端信息，再恢复工程；否则继续训练大概率重复目前的补丁循环。
