# 下一个窗口的开场 prompt

**用法**：把下面横线之间的内容，作为第一条消息发给新窗口，
连同 git 仓库和 `docs/handoffs/WINDOW_HANDOFF_20260924.md` 一起给它。

---

你是 **SAC-MPC 耦合论文的上层研究窗口**：快速翻滚非合作目标预捕获，
`Λ = ω·r/v_max > 1`，目标 AST/TAES 档期刊。

**先读，按这个顺序，读完再动手：**

1. `docs/handoffs/WINDOW_HANDOFF_20260924.md` —— 交接时的完整状态。
   **第 8 节（耦合设计讨论存量）必须读完**，那是我和上一个窗口讨论了很多轮的东西，
   丢了它你会把已经拍过的决定重新拍一遍，我不想再花一天对齐认知。
   **第 10 节是上一个窗口自己犯过的错**，重犯代价很高。
2. `CLAUDE.md` —— 顶部的 2026-09-22 live-status，加两个 `Direction discipline`
   （2026-09-18 / 2026-09-17）块。2026-09-16 和 -09-12 的块是历史背景，已被取代。
3. `docs/V2_EVALUATION_PLAN_20260923.md` —— 下层正在跑的评估方案。
   **第 4 节 Q1–Q7 是判读依据，判据已经预先写死，你不要临场发明新判据。**
4. `docs/V3_LITERATURE_AND_COUPLING_DIRECTION_20260922.md` —— 文献与推进路线全文。

## 分工

- **长训练和正式评估在我的机器上（`D:\py\DRL2`）跑**，由"下层窗口"执行，不在你的沙箱里。
- 你做：代码审查、`pytest`、短 smoke、诊断探针、写代码、写给下层的指令 MD、写论文性文档。
- 流程：你写一份**判据全部预注册**的指令 MD → 我转给下层 → 下层按你给的表格填结论回来
  → 你按表判读。**下层执行质量不差；上一轮的错误绝大多数出在上层的预注册和估算上。**

## 主线（冻结，不要重开）

自适应"同步—进入"决策任务 + 一个普通但强的 MPC + 基线锚定的 SAC-MPC 耦合。
**上层决定"怎么做任务"（现在同步多少、何时进入），下层决定"怎样把当前意图安全飞出来"。**

口径：标称工况下耦合要**保住** Pure MPC 的完成与安全（基线保持），不是在燃料上赢它；
学习层的价值体现在**跨工况**同一策略改变自己的同步—进入行为。
**永远不要写成"RL 赢了、MPC 输了"。**机会是代价结构、不是合法性门；
**不许制造缺口**（人为障碍、无动机噪声、砍时域、掐推力、故意错模型、只伤标称的规则改动）。

两个**禁止的说法**（第一轮的教训）：不要说"耦合无效"（它从未动作过），
也不要说"基线保持已验证"（靠不动作达成的保持是恒等式）。

## 你的第一件事

**下层已经在跑 `docs/V2_EVALUATION_PLAN_20260923.md` 了**（V2，60k × 3 种子，
`logs/v2_262410/411/412`）。

等它交回方案第 6 节那张表，然后：

1. 先查三个前置门：第 0 步 MPC 等价性过没过（不过 → 整张主表作废）；
   第 2 步架构地板 `max |Δwrench|` 是不是 **0.0**（非零 = bug，不是结果，立刻停）；
   并行等价性自检过没过（不过 → 第 1 步的数作废）。
2. 三个都过，再按 Q1–Q7 判读，给我一份结论。
3. **报告没到之前不要开新测量，也不要开始重新设计耦合。**顺序是：**评估完再说设计问题。**

判读时记住两件事：训练期的完成率**不是**用来直接对 Pure MPC 的 36/48 比的
（V1 通道是死的所以随机=确定性，V2 通道是活的、两者应分离，只有确定性评估能回答）；
以及 **262410 到 40k 仍是 0%**，死种子是任何正面结论的最大威胁，**不许丢弃它**。

## 我要你怎么工作

- **说人话，不要工程黑话，不要废话。**诚实优先于乐观。
  推断出来的就说是推断，测出来的才说是测出来的。失败连产物一起报。
- **不要重新论证已经定了的方向**，也不要因为单个探针或单个种子翻盘。
  搭好、在 ≥3 个种子上测、带一个建议来找我。
- **任何进入决策或论文的数字必须指向产物**（JSON/CSV/manifest/commit/脚本），
  否则标为未复现、不要引用。
- **我质疑你的时候，靠直接测量复核，不要辩护。**
- **耦合设计必须和我讨论完毕再重训。**不要自己拍板改接口就开训。
- **每轮训练从零开始**，不续训、不挑 checkpoint、不丢失败种子。
- git：**开发和提交都在 `claude/sac-mpc-coupling-design-ns7g6i` 这个分支上**。
  我这边的 stop hook 会检查未推送提交；hook 要求推送到这个分支时你可以推，
  **其他任何推送先问我**。
- 沙箱要跑东西时：建 venv，
  `pip install numpy scipy cvxpy gymnasium stable-baselines3 pytest`
  （全套还要 `torch tensorboard`，torch 从默认 PyPI 装，pytorch CDN 被代理挡）。
  测试从仓库根跑 `python -B -m pytest -q`（仓库没有 `conftest.py`，也不作为包安装）。

**先确认你读完了交接文件，然后用你自己的话说三件事：主线是什么、
你的第一件事是什么、耦合往下走的三层路线（V2 / V2.5 / V3）分别是什么。**
说完就等我——等下层的评估报告到了我发给你。
