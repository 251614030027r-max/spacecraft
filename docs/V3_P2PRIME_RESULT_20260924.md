# V3 P2′ 结果：策略层选择（各自继续），**通过**

*2026-09-24，上层窗口，沙箱。按 `docs/V3_P2PRIME_PREREGISTRATION_20260924.md`（提交 `7cb3299`，先于任何 P2′ rollout）判读。*

产物：
- `eval/adp/v3_p2prime_rollouts.json`（120 条精确 rollout），SHA-256 `f24d23abb45e72e1e3105f67a5ca9811d46c0a2c4e5c506d9500fe64cc7d6f31`；
- `eval/adp/v3_p2prime_readout.json`，SHA-256 `d63a450a671cc60f38cca4c9a5546c2f68567dda4b1f53a248a73a4e8efd50da`；
- 脚本：`experiments/probe_base_policy_rollout.py`、`experiments/analyze_v3_p2prime.py`。

被测规则：在状态 s 上按 `argmax{ V^learned(s), V_b(s) }` 选择"学习策略继续到底"或"交回 Pure MPC 到底"，
比较对象是决策级 γ = 0.99 的精确折扣回报。

## 结果

| 组 | 判据 | 结果 | 判定 |
|---|---|---|---|
| A：保护侧，66 个新回合（43 destroyed + 23 retained，含死种子 262410 的 12 个），s_0 | 丢掉的基线完成 ≤ 1 | **0 / 66** | 通过 |
| B：救回侧，9 个救回回合，新检查点 k = 10 | 保住救回 ≥ 7 | **9 / 9** | 通过 |
| B：同上，k = 20 | 保住救回 ≥ 7 | **9 / 9** | 通过 |

另报：
- A 组没有任何错判（有完成分支却选了失败分支：0 / 66）；
- 23 个 retained 回合中，有 10 个学习策略的回报更高（在两者都完成的情况下），这时选择会采用学习策略；
- 死种子 262410 的 12 个 destroyed 回合：选择全部退回 Pure MPC，丢失 0；
- B 组交回 Pure MPC 的结局：k = 10 时完成 3 / 超时 5 / 飞出 1；k = 20 时完成 5 / 超时 4。
  也就是说，**同一个学习策略在不同时刻被交回，结局差别很大**，而按价值选择在两个检查点都保住了全部 9 个救回。

## 结论

**采用策略层选择作为 V3 仲裁结构**，冻结 V3 方法执行单（`docs/V3_METHOD_EXECUTION_ORDER_20260924.md`）。

## 边界（必须和结果一起报）

1. 这些都是**精确价值**下的结果。部署时的价值要靠学习（V_L、V_b 的固定策略评估），会有误差。
   所以执行单里有 M6 校准检查：学到的价值与精确 rollout 比，符号正确率 ≥ 80% 才进正式评估。
2. P1 的已知局限仍在：临近时限的完成可能被停在抓取点的超时压过（P2 中 1 例）。本组 0 例，但不代表不存在。
3. 样本来自 V2 的两个活种子与一个死种子，全部在评估块 262000–262047 内。正式论文数字要换新的种子块。
