# 第二阶段任务边界与研究主线约束

> 更新日期：2026-08-11  
> 用途：作为后续 SAC–MPC 阶段的大方向指导与约束文档。接受此文件窗口应优先阅读本文，再讨论具体耦合方法、约束形式和实现步骤。  
> 当前默认前提：\*\*Pure SAC 基线与 MPC-only 基线均已完成并可作为独立对照\*\*。本文不再讨论两者的训练、调参和基线验收细节。

\---

## 1\. 本文首先解决什么问题

第二阶段不应从“怎样把 SAC 和 MPC 拼起来”开始，而应从任务本身开始：

> \*\*面向快速翻滚非合作目标的高保真六自由度近距离交会任务，在执行器和近距离任务约束存在的条件下，利用 SAC 与 MPC 的互补能力，提高任务收敛性能，并兼顾控制消耗、约束满足和在线计算负担。\*\*

项目主体是**任务型研究**，不是以提出复杂优化算法为主要目标的算法型研究。

因此，后续所有方法讨论都必须先回答：

1. 该设计解决了当前任务中的什么实际问题？
2. 为什么单独 SAC 或单独 MPC 在这个问题上存在不足？
3. SAC 与 MPC 分别提供了什么不可替代的信息或能力？
4. 这种配合能否通过收敛、控制消耗、约束和计算量等指标被清楚验证？
5. 方法结构是否足够可解释，后期是否还有可能进行可行性、稳定性或安全性分析？

如果某个设计只能回答“算法上更复杂”“优化器算得更精细”“耦合看起来更深”，但无法回答上述问题，则不应成为主线。

\---

## 2\. 研究对象与任务类型

研究对象保持为：

**快速翻滚非合作目标下的航天器近距离六自由度交会/接近控制。**

基础状态仍围绕 SE(3) 相对位姿及相对速度建立，核心状态可统一理解为

\[
x=\[\\phi,\\rho,\\omega\_r,v\_r],
]

控制输入为六维力矩和力

\[
u=\[\\tau,F].
]

研究重点不是单独的轨道控制或单独的姿态控制，而是**姿态—平动统一的六自由度任务收敛**。

任务环境应继续保留现有高保真特征，例如：

* SE(3) 姿轨统一表示；
* 目标快速自由翻滚；
* 完整刚体动力学；
* 地球中心引力；
* J2 摄动；
* 重力梯度相关效应；
* 六维力/力矩执行；
* 数值积分得到的绝对运动和相对运动。

这些内容构成论文场景和任务难度的重要基础，而不是为了增加模型复杂度本身。

当前名义环境已经能够支持 Pure SAC 与 MPC-only 独立完成任务，因此后续研究不是再次证明“这个任务能控制”，而是把任务推进到**带有实际近距离操作约束和综合性能要求的控制问题**。

\---

## 3\. 为什么采用 SAC + MPC

采用 SAC 与 MPC 的理由必须建立在二者的能力差异上，而不是因为“RL + MPC”本身是一个研究方向。

### 3.1 SAC 的基本定位

SAC 是通过大量高保真仿真交互获得的**学习型任务策略**。

它的潜在价值主要是：

* 能从大量状态中学习复杂非线性任务行为；
* 单次网络前向计算速度快；
* 能形成一定的长期控制经验，而不是只依据当前局部模型；
* 可能在任务时间、控制使用方式、非线性大范围状态处理等方面形成有价值的策略。

其不足也很明确：

* 网络动作本身缺乏显式约束保证；
* 对训练分布外状态的行为难以直接解释；
* 很难仅依靠策略网络保证未来一段轨迹始终满足接近走廊、视场、接近速度等工程约束；
* 稳定性和可行性分析通常较困难。

因此 SAC 不应被简单理解为“一个比 MPC 快的控制器”，而应理解为**已经学习了任务经验的信息源和控制策略基线**。

### 3.2 MPC 的基本定位

MPC 是**模型型预测控制基线**。

现有 MPC-only 已经证明，在当前名义模型匹配条件下，利用高保真动力学局部信息和输入约束，可以可靠完成基础交会任务。

MPC 的主要价值是：

* 能显式利用物理模型预测未来；
* 能将输入和状态约束写入优化问题；
* 控制决策过程相对可解释；
* 容易与可行性、稳定性、安全性等控制理论分析衔接。

其潜在不足主要体现在：

* 在线预测和优化存在计算负担；
* 预测模型必须在精度和计算量之间折中；
* 有限预测时域不一定包含足够的长期任务信息；
* 模型失配、扰动和复杂约束可能降低性能；
* 当任务复杂度增加时，单纯依赖在线优化可能出现更高计算成本或更保守的控制行为。

### 3.3 两者结合的基本研究假设

第二阶段的基本研究假设是：

> \*\*SAC 所包含的学习经验与 MPC 所提供的模型预测和显式约束能力具有互补性。合理的信息交互有可能在不依赖过度复杂算法的情况下，提高高保真近距离任务中的综合控制性能。\*\*

这里的“综合性能”不是单一成功率，而是至少同时考虑：

* 是否完成任务；
* 收敛是否更快；
* 控制消耗是否更合理；
* 约束是否得到满足；
* 在线计算是否可以接受。

后续具体怎样交互，**本文不提前锁定**。

\---

## 4\. 当前阶段不提前定义 SAC 与 MPC 的具体耦合方式

这是本文最重要的边界之一。

目前只固定：

> \*\*最终方法属于 SAC + MPC 的受约束协同控制框架。\*\*

暂时不固定：

* SAC 是否提供动作、轨迹、终端信息或其他辅助信息；
* MPC 是否对 SAC 进行修正、参考、预测增强或其他形式的利用；
* 是否需要再次训练 SAC；
* 是否需要 critic；
* 是否采用单候选、双候选或其他机制；
* 是否采用 RTI、SQP、NMPC 等具体求解形式；
* 是否需要复杂的在线模型修正。

这些问题必须结合：

1. Pure SAC 与 MPC-only 的实际性能特点；
2. 当前任务真正暴露出的不足；
3. 权威方法文献；
4. 计算复杂度和实现代价；

再决定。

禁止为了追求“耦合深度”而提前强行设计复杂双向网络或复杂优化器。

\---

## 5\. 论文真正要解决的核心问题

第二阶段论文应尽量围绕以下四个问题展开。

### 问题 A：高保真快速翻滚六自由度任务如何稳定收敛？

这是最基础的任务能力。

必须保持对：

* 相对位置；
* 相对姿态；
* 相对线速度；
* 相对角速度；

的统一控制。

Pure SAC 和 MPC-only 已分别提供基础证据，后续协同方法不能以牺牲基本收敛能力为代价换取所谓“算法创新”。

### 问题 B：加入近距离任务约束以后，如何仍然完成任务？

当前执行器输入限制已经存在。

后续任务可逐步考虑具有明确工程含义的约束，例如：

* 接近走廊；
* 传感器视场；
* 接近速度；
* 必要时的安全距离或禁入区域；
* 六维控制输入限制。

这些约束必须服务于“近距离交会/接近任务”的物理意义，而不是为了让优化问题人为变难。

不要求一次性加入所有约束。应优先选择最能说明任务意义、实现复杂度合理的约束组合。

### 问题 C：能否改善任务效率与资源消耗？

论文不应只比较成功率。

至少需要关注：

* 完成时间；
* 位置和姿态收敛速度；
* 终端速度误差；
* 力控制使用量；
* 力矩控制使用量；
* 控制饱和情况。

“省油”必须严谨表达。

若没有推进系统和真实质量流率模型，则不能直接把简单的二次控制代价称为真实燃料消耗。可以使用：

* 力冲量；
* 力矩冲量；
* 控制量积分；
* 二次控制能量代理指标；

作为**控制消耗/推进剂消耗代理指标**。

如果后期需要强化“燃料”表述，应再建立合理的推进剂消耗模型。

### 问题 D：在线计算负担是否合理？

在线计算是工程评价指标之一，但不是当前论文的唯一核心。

应报告：

* SAC 网络推理时间；
* MPC 求解时间；
* 若存在额外预测/耦合模块，其计算时间；
* 完整控制周期总时间；
* 平均值、中位数、P95 等统计量。

“轻量化”必须由实际结果支撑。

只有当计算量确实成为瓶颈时，才值得进一步研究减少预测次数、缩短时域或简化求解等问题。

禁止先人为制造一个很重的 MPC，再以“降低计算量”为主要创新。

\---

## 6\. 高保真环境在论文中的作用

高保真环境不是论文的附属仿真器，而是整个课题的重要支撑。

它主要承担三个作用。

### 6.1 提供可信的复杂任务环境

使 SAC 与 MPC 不只在理想线性相对运动模型上比较，而是在：

* 姿轨耦合；
* 快速翻滚；
* 非线性轨道环境；
* 六自由度执行；

条件下完成任务。

### 6.2 为学习策略提供真实的训练经验

SAC 的价值来自大量高保真交互，因此其任务经验本身与高保真环境绑定。

后续如果使用 SAC 提供任何形式的信息，必须保持其物理语义清楚。

### 6.3 检验在线控制方法面对模型简化和模型失配时的能力

控制器内部使用的预测模型不要求与 truth model 完全相同。

高保真 truth model 与可实时运行的控制预测模型之间存在合理差异，本身就是工程现实。

但不能故意把 MPC 模型削弱到明显不合理，再通过 SAC“补救”，以此制造性能提升。

任何模型简化都应有明确理由：

* 在线计算；
* 建模可得性；
* 控制器结构；
* 后续理论分析。

\---

## 7\. 方法设计必须遵守的原则

后续具体方法可以变化，但必须满足以下原则。

### 7.1 任务驱动

每增加一个模块，都必须对应一个明确任务问题。

例如：

* 解决有限预测导致的长期信息不足；
* 解决模型失配；
* 改善约束下的收敛；
* 降低计算量；
* 改善控制消耗。

不能以“让结构更复杂”为理由增加模块。

### 7.2 信息交互必须有意义

SAC 与 MPC 不能只是形式上的串联。

最终必须能够解释：

* SAC 向 MPC 提供了什么；
* 为什么这个信息是 MPC 自身难以低成本获得的；
* MPC 如何利用该信息；
* MPC 又保留了哪些由模型和约束带来的优势。

即使最终方法很简单，只要这一信息分工合理、可验证，也优于一个难以解释的复杂组合。

### 7.3 物理语义清楚

学习模块输出和 MPC 使用的信息尽量具有明确物理含义。

避免无必要地设计大量不可解释的网络参数、隐变量或动态权重。

### 7.4 控制结构可拆解

最终方法应能够自然拆成：

* Pure SAC；
* Pure MPC；
* SAC–MPC；

并能够通过消融实验说明“加入学习信息以后到底改变了什么”。

### 7.5 不追求复杂优化算法本身的创新

原则上不把以下内容作为主贡献：

* 新 QP/NLP 求解器；
* 复杂 KKT 反向传播；
* 大规模可微 MPC；
* 为了减少几次迭代而设计复杂数值技巧；
* 复杂神经网络调权重系统。

若后续确有必要，可以作为实现细节或次要改进，而不是主线。

\---

## 8\. 可解释性与后期理论分析要求

虽然当前阶段不要求立即完成严格证明，但最终方法应从一开始避免把理论分析的可能性堵死。

### 8.1 可解释性要求

最终控制动作应尽量仍能解释为：

> 学习信息 + 模型预测 + 约束优化 → 实际六维控制。

应避免最终控制完全由多个黑箱网络混合输出，使得无法区分哪个模块承担安全、收敛或性能作用。

### 8.2 可行性要求

后期如果加入状态约束，应优先考虑能够明确讨论：

* 约束可行性；
* 软约束与松弛变量；
* 不可行时的回退策略；
* 递归可行性的可能条件。

不要求第一版立即证明递归可行性，但软件和算法结构不应完全依赖“求解失败就给零控制”这类不可分析机制。

### 8.3 稳定性要求

后期可以根据最终方法选择合适层次的理论分析，例如：

* 名义闭环稳定性；
* 局部稳定性；
* 基于 Lyapunov/终端代价的分析；
* 次优 MPC 性能界；
* 有限误差条件下的实用稳定性；
* 安全集合前向不变性。

具体采用哪一种，必须根据最终耦合结构决定。

当前不提前承诺“全局渐近稳定”“严格安全保证”等结论。

\---

## 9\. 约束的边界

约束是本课题的重要组成部分，但不应发展成另一篇纯安全控制论文。

当前优先级建议保持：

1. **输入约束**：已有，必须始终保留；
2. **接近方向/走廊约束**：具有明确近距离任务意义；
3. **传感器视场约束**：具有导航可用性意义；
4. **接近速度约束**：体现终端安全接近；
5. **必要时再加入安全距离/KOZ 等约束**。

CBF、双层 MPC、复杂动态障碍物等方法暂不作为默认要求。

只有当普通预测约束不足以解决任务，或者后期论文确实需要更严格安全保证时，再讨论 CBF 等结构。

\---

## 10\. 对“创新”的当前定义

当前不把以下内容单独视为足够的创新：

* 第一次把 SAC 和 MPC 放在一起；
* RL 上层规划、MPC 下层跟踪；
* SAC rollout 后 MPC 再优化；
* 用神经网络在线调几个 MPC 权重；
* 单纯把机器人/车辆中的 RL–MPC 方法换成航天器；
* 单纯把线性模型换成 SE(3) 模型。

真正希望形成的是一种**任务驱动的组合贡献**：

> 在快速翻滚非合作目标的高保真六自由度近距离任务中，围绕收敛、控制消耗、近距离约束和在线计算之间的矛盾，设计一种结构清晰、可解释的 SAC–MPC 协同控制方法，并系统展示其相对 Pure SAC 与 Pure MPC 的综合优势。

对于研究生阶段以二区期刊为目标的工作，不要求必须提出高度复杂的新优化理论。

更现实的贡献组合可以是：

1. **任务与模型层面**：高保真快速翻滚六自由度受约束近距离任务；
2. **方法层面**：一个有明确职责分工和有效信息交互的 SAC–MPC 框架；
3. **性能层面**：在收敛、控制消耗、约束和计算量上给出充分对照；
4. **可信性层面**：后期增加适当的模型失配/扰动和可行性或稳定性分析。

是否最终达到二区要求不能提前保证，但论文应按这一完整程度组织，而不是依靠单一复杂算法点支撑。

\---

## 11\. 论文评价指标边界

后续实验至少应覆盖以下类别。

### 11.1 任务完成与收敛

* 完成率；
* 任务完成时间；
* 末端位置误差；
* 末端姿态误差；
* 末端相对线速度；
* 末端相对角速度。

### 11.2 控制消耗

* (\\int |F|dt)；
* (\\int |\\tau|dt)；
* 必要时 (\\int |u|^2dt)；
* 力/力矩饱和比例；
* 若建立推进剂模型，再报告真实或近似燃料消耗。

### 11.3 约束

根据最终采用的约束报告：

* 约束满足率；
* 最小安全裕度；
* 违规次数；
* 违规持续时间；
* 松弛变量使用情况。

### 11.4 计算

* 网络推理时间；
* MPC 求解时间；
* 耦合模块额外时间；
* 完整控制周期总时间；
* P50/P95/最大值等。

论文不能只展示一条漂亮轨迹。

\---

## 12\. 必须保留的对照关系

最终论文至少保持：

### Pure SAC

代表学习控制能力。

### Pure MPC

代表模型预测控制能力。

### SAC–MPC

代表最终协同方法。

这三者必须：

* 使用相同基础任务；
* 使用相同执行器限制；
* 使用相同初始条件集合或共享 Monte Carlo 种子；
* 使用一致的成功判定；
* 使用一致的指标定义。

若后期增加约束，需要明确 Pure SAC 如何处理这些约束。

如果 Pure SAC 本身未设计硬约束机制，则应如实报告约束违反，而不是人为修改评价方式使其看起来公平。

如果最终方法增加了明显额外计算，也必须将计算成本一起报告。

\---

## 13\. 鲁棒性研究的边界

鲁棒性属于后期完整性验证，不应过早扩张项目。

在名义受约束方法已经明确有效之后，可以选择少量具有物理意义的失配：

* 质量误差；
* 转动惯量误差；
* 推力/力矩执行效率偏差；
* 有界小外扰；
* 必要时测量噪声。

目的不是建立一个极端 worst-case 鲁棒控制问题，而是回答：

> SAC–MPC 的性能是否只存在于完全模型匹配环境？

扰动和不确定性范围必须合理、透明，不能为了突出某种方法而人为设定。

\---

## 14\. 当前文献的作用边界

现有文献已经足够支持当前大方向，但不锁定具体方法。

### 哈工大 TAES 2025

主要作用：

* SE(3) 快速翻滚目标 DRL 控制背景；
* 高保真姿轨耦合任务的相关工作基础。

不作为当前工程的严格复现目标。

### 北航 TAES 2024

主要作用：

* 近距离任务中的接近路径、FOV、输入饱和和扰动问题；
* 支撑后续约束选择具有航天任务意义。

### 北航 TAES 2026 编队

主要作用：

* 说明 RL 与 MPC 在航天任务中可以承担不同信息层次；
* 有限星载计算和约束控制的研究背景。

其 CW 编队模型和具体算法不直接迁移。

### 南航 AST 2026

主要作用：

* 快速翻滚/失效目标下受约束 MPC、安全与收敛；
* 后期约束和稳定性/安全性分析参考。

不要求照搬双层 CLF–CBF MPC。

### 上海交大 AST 2026

主要作用：

* 说明 SE(3) 航天器 RL–MPC 已经存在“学习 MPC 代价”的相关路线；
* 帮助划定 novelty boundary。

不作为当前主要实现模板。

### ECC 2026 Rollout Then Optimize

主要作用：

* 说明学习策略的未来 rollout 可以用于部署阶段的模型优化修正；
* 支撑“学习策略可以向 MPC 提供未来信息”的方法背景。

不要求照搬单步 Newton/RTI 结构。

### AC4MPC, IEEE TCST

主要作用：

* actor/critic 与 MPC 协同的代表性方法；
* 提供 actor rollout、长期价值和 MPC 在线优化之间关系的理论与工程参考。

不要求完整复现 AC4MPC。



如需要寻找新的方法性论文，应重新严谨筛选，并重点寻找：

> \*\*能够解释 RL 与 MPC 如何有效交换信息、但不依赖过度复杂训练和求解结构的方法。\*\*

场景可以来自机器人、车辆、无人机等领域，但应确保其耦合思想可迁移，而不是仅因为应用名称不同。

\---

## 15\. 后续讨论的“防跑偏规则”

后续任何窗口在提出新方法前，应先检查以下规则。

### 规则 1：不从算法名字出发

错误思路：

> “要不要做 RTI？”  
> “要不要做 differentiable MPC？”  
> “要不要上双候选？”

正确思路：

> “当前任务具体缺什么能力？这个方法是否正好解决？”

### 规则 2：不因为别人算法复杂就认为我们也必须复杂

本课题优势来自：

* 高保真任务；
* 六自由度姿轨耦合；
* 快速翻滚目标；
* 真实约束；
* 学习和预测控制的互补。

不是来自优化器复杂度。

### 规则 3：不为了证明 SAC 有用故意削弱 MPC

如果 Pure MPC 已经很好，应接受这一事实。

SAC 的价值必须来自真实任务互补，而不是人为缩短 MPC 时域、降低迭代或使用明显不合理的预测模型后再“救回来”。

### 规则 4：不为了证明 MPC 有用故意让 SAC 违反约束

约束应来自任务本身。

Pure SAC 若违反约束，应作为方法能力边界如实报告。

### 规则 5：若大方向不成立，允许调整而不是死磕

如果后续严谨实验表明：

* Pure MPC 在全部核心指标上都明显优于 SAC；
* SAC 提供的信息无法形成可重复收益；
* SAC–MPC 只能通过复杂数值技巧获得极小提升；

则应重新评估耦合方式，必要时调整任务或方法。

不能因为已经选择“RL–MPC”就无限追加小优化。

\---

## 16\. 当前论文主线的最简表述

后续所有讨论都可以用下面这段话检查是否偏离：

> \*\*本文面向快速翻滚非合作目标的高保真六自由度近距离交会任务，考虑执行器及典型近距离运动约束。Pure SAC 通过高保真环境学习复杂任务经验，Pure MPC 利用模型预测实现可解释的在线优化与约束处理。在此基础上研究二者如何进行有效的信息交互，使协同控制在保持任务收敛能力的同时，进一步兼顾收敛速度、控制消耗、约束满足和在线计算开销。方法设计强调结构清晰和物理可解释，并为后续可行性、稳定性或安全性分析保留空间。\*\*

如果后续方法已经无法用这段话自然解释，说明项目可能开始偏离主线。

\---

## 17\. 窗口的起点

下个窗口**不要立即开始写新的 SAC–MPC 代码**。

首先只做两件事：

1. 基于本文重新确认研究问题和评价目标没有变化；
2. 在已有 ECC、AC4MPC、TAES、AST 文献基础上，选择少量真正具有“RL–MPC 有效信息耦合”参考价值的方法文献。

随后再讨论：

* 当前任务中 SAC 与 MPC 最值得交换的到底是什么信息；
* 哪种配合方式最简单、最可解释、最容易形成有效对照；
* 怎样在不过度增加工程复杂度的情况下形成足够的论文方法贡献。

在完成这一判断之前，不锁定具体算法。

\---

# 一页式边界总结

**研究对象**  
快速翻滚非合作目标，高保真 SE(3) 六自由度近距离交会。

**基础方法**  
Pure SAC + Pure MPC 已完备。

**最终方向**  
SAC + MPC 受约束协同控制。

**任务目标**  
收敛可靠、时间合理、控制消耗较低、约束满足、在线计算可接受。

**任务约束**  
输入限制为基础；后续优先考虑接近走廊、FOV、接近速度等具有明确工程含义的约束。

**方法要求**  
有真实信息交互，不是简单算法拼接；结构清楚；物理语义明确；能够做消融。

**不追求**  
新求解器、复杂可微 MPC、大规模联合训练、纯数值优化技巧。

**理论要求**  
第一版不要求严格证明，但算法结构必须为后续可行性、稳定性/安全性分析留出空间。

**论文定位**  
任务型、工程控制型研究。以高保真场景 + 合理 SAC–MPC 协同 + 约束 + 收敛/消耗/计算综合评价形成完整贡献，目标为满足研究生毕业所需的较高质量期刊工作，具体分区不预先保证。

**防跑偏原则**  
先问“任务缺什么”，再问“算法怎么做”；如果大方向证据不足，允许调整，不死磕局部优化。

