# DRL2 整理与精简记录

日期：2026-08-10

## 空间结果

- 整理前：约 9.617 GiB。
- 整理后：4.760 GiB。
- 减少：约 4.857 GiB，项目体积下降约 50.5%。
- 整理后 `.venv` 占 4.750 GiB；其中主要是带 CUDA 的 PyTorch，属于当前训练环境，未删除。
- `legacy/` 约 10 MiB，包含一份参考模型、关键验收证据、旧文档与旧源码快照。

经用户明确确认，永久删除了隔离目录 `legacy/bulk_delete_pending` 中的 854 个旧文件，内容包括全部非选中模型/检查点、旧回放池、旧训练日志、TensorBoard 历史及原 `archive/` 大体积归档。Windows 首次删除遗留两个超长路径 TensorBoard 文件，随后用长路径 API 删除并确认目标目录不存在。这些大体积历史产物已无法从本机恢复；用户已说明另有保存。

## 保留内容

- 活动 `.venv` 和 CUDA PyTorch 环境。
- 精简后的 22 个活动 Python 模块、11 个测试文件和 4 份活动说明文档。
- `legacy/reference_model/sac_reference_20260817_200k.zip`（8,992,468 bytes）。
- `legacy/reference_baseline_evidence/` 下的最终验收、共同种子评估、选中训练清单及监控记录。
- `legacy/old_source/` 与 `legacy/documents/` 下的小体积旧源码和旧文档，用于追溯，不参与活动运行。

## 活动设计清理

活动代码已移除 PPO/TD3、算法/环境 profile 切换、L0/L1/L2/Level-C 自动化入口、旧模型继承、回放池恢复、论文原样奖励、失败定值惩罚、剩余时域补偿、扰动/不确定性兜底和旧评估探针。当前只有一个名义六自由度环境、一个三项物理储能奖励、一个 SAC 配置、一个从零课程训练入口和一个正式评估入口。

## 验证证据

- 安装前隔离镜像：38/38 测试通过。
- 安装后活动工程：38/38 测试通过。
- 项目 `.venv` 的 CPU 正式配置验证通过。
- 文档所列 GTX 1650 CUDA 训练命令以 `--validate-only` 原样执行通过，耗时约 14 s；未启动训练、未创建模型或训练日志。
- 活动源码扫描未发现 `PPO`、`TD3`、旧论文复现模式、旧失败补偿或 `--init-model` 入口。

本次没有训练最终模型。`models/` 与 `logs/` 已清空，等待用户按操作手册执行唯一一次从零正式训练。
