# 非合作探针 2 正式结果：估计控制与真值控制

核验日期：2026-09-16 JST。本文记录固定设定点 Pure MPC 在同一感知环境、同一 48 种子块下使用 EKF 估计或真值控制的配对结果，并按 B 完成前锁定的上层条件裁决判定下一步。本文是结果记录，不授权新的训练或任务修改。

## 结论

探针 2 命中上层预签的**指令 1：基本持平，停止本方向的探针 3 与训练**。估计控制 A 完成 31/48，真值控制 B 完成 32/48，主指标 `ΔC = B − A = 1 ≤ 2`。当前相机/EKF 的失明滑行只破坏 1 个配对成功种子，没有制造足够的完成率余量来支撑后续学习或基线保持探针。

估计控制的成功燃料比真值控制高约 9.25%，非法入口多 1 次，并出现分散的 7 个 QP 不可行步；但两行都为 0/48 真值违约，A 最长连续零推力仅 1 步，QP 事件没有形成级联或造成额外配对破坏。这是轻度代价，不是实现崩溃，也不改变锁定的完成率主门槛。

## A/B 正式结果

| 指标 | A：EKF 估计控制 | B：真值控制 | A 相对 B |
|---|---:|---:|---:|
| 完成 | 31/48 | 32/48 | −1（−2.08 个百分点） |
| 成功平均时间 | 78.677 s | 78.825 s | −0.19% |
| 成功平均力冲量 | 205.087 N·s | 187.721 N·s | +9.25% |
| 真值违约局 | 0/48 | 0/48 | 相同 |
| 最差归一化真值裕度 | +0.000704 | +0.011904 | A 更紧但仍为正 |
| 非法入口穿越 | 19 | 18 | +1 |
| 有非法入口的局数 | 18 | 17 | +1 |
| QP 不可行步 | 7（2 局） | 0 | +7 |
| 最长连续零推力 | 1 步 | 0 步 | +1 步 |
| 控制器平均 / p95 | 70.32 / 76.97 ms | 71.24 / 78.44 ms | 均低于 100 ms 控制周期 |

B 精确复现历史 truth Pure MPC 的 32/48 与 78.825 s，成功平均力冲量也与历史 187.721 N·s 一致，说明同感知环境的 truth 控制锚点健康。

## 配对分布与失败归因

| 配对状态 | 种子数 |
|---|---:|
| A 成、B 成 | 31 |
| A 败、B 成 | 1 |
| A 成、B 败 | 0 |
| A 败、B 败 | 16 |

唯一由估计控制破坏的种子是 `262003`：B 完成，A 超时；A 有 1 次非法入口，QP 不可行 0 步、最长零推力 0 步、真值违约 0。它归入上层定义的 **(i) 真·失明代价**，不是实现假象。

A 的 17 个未完成局全部为超时、无距离失败、NaN 或真值违约。其中 16 个在 B 中也未完成，因此属于共同任务失败，不能归因于估计；`262011` 和 `262036` 分别有 1、6 个 QP 不可行步，但 B 同样未完成，且 A 最长零推力只有 1 步，没有不可行级联。按上层定义，**(ii) 实现假象为 0 个配对破坏种子**。

## 实验完整性

- A/B 都使用 `desired_pose`、`--perception`、48 个唯一种子 `262000–262047`、h35 和 `arrival_condition`，串行单进程运行；唯一实验因子是 `--control-source estimated` 对 `truth`。
- 动力学、奖励、完成与七类安全违约始终由真值裁决；两份 JSON 均完整解析，48 个种子无重复，stderr 为空，接力退出码为 0。
- A：`D:\py\DRL2\logs\precap_noncoop\pure_mpc_estimated_48.json`；B：`D:\py\DRL2\logs\precap_noncoop\pure_mpc_truth_48.json`。原始 JSON 本地保留，按上层裁决不推入公开 Git 仓库。
- 当前 JSON 的 `source` 只记录 `desired_pose`，没有单独序列化新加的 `perception/control_source` 字段；本报告据实际命令和受控 A→B 串行接力记录两条路径。后续若复用评估器，应先补齐这两个 provenance 字段，但本轮不为元数据字段重复 96 局。

完整回归为 `236 passed in 168.08s`。上层补丁报告 235；多出的 1 项来自先前提交 `1111e8e` 新增的独立测试 `test_truth_violation_step_counts_preserve_missing_fields`。同一提交对主表测试增加的检查是既有测试内断言，不增加测试计数。

## 锁定动作

依据预签裁决，当前停止：不构建或执行探针 3、不训练、不通过加噪声、缩视场或改任务人为制造 gap，不改 MPC、奖励与 T12 六模型。下一步回到上层重新定义真正需要长期决策的观测/相位耦合问题。
