# 本地执行交接 — T12 正在训练

2026-09-13更新；以本文件描述的本地运行事实补充WINDOW_HANDOFF_20260913.md。用户要求只落地推送、当前训练不变；新窗口不要重复启动或改变参数。

六个运行均从a050580启动，各60000决策，显式h35、20控制步/决策、无反馈。arrival_condition种子262400/262401/262402，观测32动作2，棘轮开启；radial_local种子262500/262501/262502，观测31动作4。环境保留相位/剩余时间观测。训练启动时228 passed in 182.13s，前序T11为224 passed。

运行目录D:/py/DRL2/logs/t12_train，launch_state.json记录启动时间、PID与命令，各运行manifest.json记录配置；train.monitor.csv及stdout用于进度，final_model.zip才是正式评估模型。进程PID属启动快照，须实时验证。低频监视自动任务t12已在原窗口运行，不要再建重复监视。用户只要求NaN、异常退出或全完成通知；失败种子不丢、不换、不擅自重跑。

后续manifest补充字段与执行文档提交未改变训练语义，不需重训；现有进程仍用启动代码，不会动态添加新的manifest字段。评估arrival使用 --horizon 35 --parametrization arrival_condition --phase-time-observation；radial使用 --horizon 35 --parametrization radial_local --phase-time-observation；均不加execution-feedback。六final模型各48种子262000–262047 deterministic，按三种子分布报告，结果型评估可并行；compute独占串行另测。Pure SAC本轮不做。

基准新门控h35是32/48、成功均时78.825s、均燃料187.721 N·s、非法穿越18、QP不可行0；历史12子集8/12。执行单§2的9/12、88.5、167.2属于旧门控历史，不能拿来证明新控制器跨路径逐位一致。棘轮端点上层12局字段与本地基准子集关键字段相同；未在本地另跑12×300逐wrench验证，不把episode字段相同冒充该项测量。

T11反向代价192局已完成，净完成T20/40/60/80/100/120=32/25/19/18/21/18。旧判据触发停止后，上层T12改为最佳常数32对oracle40而裁通过；原报告保留历史语境。新增262028/T60有1个不可行步，最长连续零推力1步；旧262039/T120有69步。原156局缺最长零推力字段，不填0。原始结果已通过上层T12补丁进入logs/t11_gate_b，本地相同数据logs/t11不重复入Git，stdout/stderr/模型不入Git。

报告中的oracle40/48是指定网格事后选时的参考，不能上主表，也不是任意控制策略的理论上界。T20仅追平基线不等于拿到oracle全部收益。旧T8最好7/12对MPC8/12、配对燃料贵40%保留为缺陷控制器历史证据。不同机器的毫秒/100ms比值仍依赖硬件，不能消除硬件差异；未来直接求解器加速幅度尚未实测，本轮不优化。

上层403与本地自动推送审批拒绝是不同问题；本次用户已重新明确要求落地推送供新窗口读取。推送只包含已有补丁、交接、必要结果汇总，不包含正在写入的训练日志或模型。实际推送是否成功以本窗口最终确认及远端HEAD为准。
