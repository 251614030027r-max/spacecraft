# Repository cleanup record (2026-09-09)

Cleanup was performed while the three v3 hybrid trainings were active. Their
processes and directories were excluded from every target list and remained
running throughout.

Removed as reproducible redundancy:

- 101 historical checkpoint ZIPs under `models/*/checkpoints/` (879.5 MB),
  while retaining every directory's `final_model.zip` or sole
  `interrupted_model.zip`;
- obsolete absolute-action hybrid v1 model/checkpoint/TensorBoard files while
  retaining each run's manifest and Monitor CSV;
- the three v2 5k checkpoints while retaining the three interrupted models
  used by D0, manifests, Monitor CSVs, and D0 output JSON;
- merged patch-application bundles (`_patches/`, `patches/`,
  `UPPER_DELIVERY*.patch`, `ACTION_SPACE_FIX.patch`, and apply instructions),
  all recoverable from Git history.

Eight superseded root handoffs were moved to
`docs/handoffs/archive_202609/`. The active root ruling was retained.

Preserved without modification:

- active `logs/hybrid/sac_mpc_hybrid_v3_macro_262100..262102` directories;
- D0, N-A, D2 and D3 evidence;
- A1/G0/A2/A3 formal evidence directories;
- historical final models and their corresponding compact logs;
- code, tests, environment, dependency files and references.

Measured space released before Git object housekeeping: approximately
1,040.2 MB. No Git garbage collection was run while training was active.
