# 当前交接：四探针收口、路线暂停与仓库入口

更新时间：2026-09-01。

## 一句话状态

值耦合、目标翻滚相位、目标模型失配和目标状态观测误差四个“寻找 hybrid 缺口”的探针，在修正观测误差注入错误后全部为负。当前没有证据支持继续投入 SAC–MPC 混合训练，研究任务已暂停，等待用户与上层重新确定下一步方向。

## 已确立的基线

- 共同基准是冻结的 S1-v2 `single_phase` 六自由度持续共旋任务；训练可分阶段，正式评价不可切换任务或控制架构。
- Pure SAC 已关闭为弱基线：三个训练种子在统一 262000 评价块上的完成率为 `3/20、1/20、0/20`，无违约率为 `20/20、10/20、0/20`。不得只报最好种子。
- corridor-guidance Pure MPC 在名义任务上完成 `20/20` 且所有真实几何裕度为正；精确真值线性化刷新会造成 p95/max 超时，但短 horizon 的 local 模型与稀疏刷新在三个相位采样块上仍全部完成且安全。
- 脚本控制器是可达性与统一指标口径参考，不是待发表方法。

## 推翻结论的错误

`env/observation_error.py::TargetStateEstimator._biased` 曾在旋转姿态估计时错误旋转目标惯性速度，把约 7.6 km/s 轨道速度的一部分注入为约 6.6 m/s 的虚假相对速度，导致推力饱和和 `total_speed` 违约。正确实现只改变姿态估计，保持惯性位置、速度和角速度不变。

修正后：姿态偏置至少到 5° 未形成缺口；真实影响来自时延/低更新率，经典模型预测可补偿。因此早先“不可观姿态偏置击穿 Pure MPC”的结论和基于它批准的训练均作废。仓库现有偏置网格日志产生于错误修正前，已隔离并标注为无效证据。

## 四个探针

完整数字、证据路径和边界见 `docs/PROBE_RESULTS.md`。结论为：

1. 学习凸终端值没有补回短 horizon 的必要，反而因径向梯度切穿弯曲走廊而有害；
2. 随机化目标初始姿态和角速度方向没有击穿 local/稀疏刷新 MPC；
3. 目标惯量 ±30% 在 1 s 预测窗内只造成约 0.01° 姿态预测误差；
4. 正确建模的姿态偏置无害，时延被经典预测补偿。

## 当前闸门

- 不启动训练，不继续实现 hybrid，不把旧的“部分可观缺口”写成现行结论；
- 不修改冻结的 S1-v2 任务参数；
- 如继续实验，只能在用户与上层重新拍板后，以单因子、独立提交和至少三个种子的方式推进；
- 可选但尚未授权的工作只有：零均值位姿量测噪声快验证，或路线 B 的诚实论文骨架。

## 工程现场

- 活动分支：`claude/sac-mpc-coupling-design-ns7g6i`；
- 最近完整回归：`146 passed`；旧交接写的 147 与现场不一致；
- 姿态偏置实现已保持惯性速度，但当前测试未单独断言这一不变量，后续若修改该模块应先补回归；
- `.venv` 启动器绑定已不存在的 Python 3.12.6；验证方式见 `docs/REPRODUCIBILITY.md`；
- `logs/`、`models/` 和 `References/` 是审计资产，本次说明整理未删训练日志、模型或论文。

## 执行纪律

严格单因子；每因子独立提交和清单；结论至少三个训练/评测种子并报告分布；每次训练从零；compute 报 p95/max；违约按真实几何判；`D:\py\DRL2` 是唯一可写工程，`D:\py\DRL` 严格只读。
