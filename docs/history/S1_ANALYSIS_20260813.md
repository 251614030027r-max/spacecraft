# Phase-2 mission S1 analysis — 2026-08-13

运行`phase2_mission_s1_seed260813`完成250,000/250,000 steps，seed 260813，fresh actor/critic/replay，fixed entropy 0.005，gamma 0.997。final model、50k间隔checkpoints、5次周期evaluation、monitor、TensorBoard和diagnostics完整保留。

正式deterministic评估使用30个固定seed 260900–260929：Gate acquisition 0/30，30/30因Phase-I总速度超过0.50 m/s终止，无premature-entry、distance或time failure。最小Gate距离中位数10.5471 m、最好5.0915 m；终止时间中位数21.5 s；动作饱和率均值0.330。该模型不合格，不得进入S2。

周期evaluation在50k、100k、150k、200k、250k的Gate成功均为0。diagnostic probe显示actor action std均值从0.355降至0.00228，饱和比例从0.208升至0.568；critic Q1均值从约4.09膨胀至489,766，Q2至490,032。TensorBoard最终actor loss约-542,614、critic loss约124,040,488，episode reward mean约-194.7。固定entropy保持0.005，但未阻止critic发散和确定性策略饱和。

轨迹reward分解确认并非Gate阈值不可达：30回合累计progress均值-85.75、state cost均值-105.72、event均为-10；策略总体远离active reference并以超速结束，不能靠增加steps修复。

下一配置仅做两个直接对应证据的变化：learning rate`2e-4→1e-4`、gradient steps`4→1`，降低每个环境transition的critic更新强度；Phase-I在0.50 m/s粗速度边界前增加同一有界warning函数，安全速度无代价、接近边界时提供连续信号。保持Gate、初始分布、reward主体、gamma、fixed entropy、网络、seed和250k预算不变。

下一S1必须从零运行，名称`phase2_mission_s1_stable_seed260813`。50k周期诊断关注Q、critic loss、actor std、动作饱和和Gate趋势；仍不因单个早期评估立即改配置。完成后用相同30-seed集合判断是否进入S2。

同时修复评估接口缺陷：manifest JSON中的三维tuple恢复为list后导致内容相同却canonical比较失败；dataclass现规范化为tuple。评估记录新增Gate入场状态、失败类型和minimum Gate distance。该修复不改变已训练模型或任务。
