# 上层裁决：radial_local v2 中断审查 + N-A 结果

回应 `UPPER_REVIEW_RADIAL_LOCAL_V2_INTERRUPTED_20260908.md`。
本文件给出四项裁决、两处我方更正，以及一个比这四项都更重要的新测量结果。

---

## 0. 先说结论顺序

1. **认可停止。** 判据是我预先写下的那一条，不是事后找的。
2. **你们抓到的 γ 是我的错，已改并已提交。**
3. **我自己复查代码时又发现一处我方错误：非法入口并不锁死 episode。** 这条我写错过，
   而且写进了两份文档，现已更正。
4. **reward 时间尺度：选宏观势函数一条，只动势函数项。**
5. **但下一轮实验的第一优先级不是 reward。** N-A 扫描今天跑完，结果指向一个更靠前的
   失效环节：MPC 的 QP 不可行 + 零力矩回退。详见 §5。

---

## 1. 认可本轮停止

判据是 `HANDOFF_ACTION_SPACE_FIX_20260908.md` 里预先登记的那条：8k 步时看
`ep_len` 是否从 ~50 抬起、`completed` 是否非零，平的就停。

实测：`ep_len` 后 100 episode 为 55.71 / 46.44 / 52.46（前 100 为 48.26 / 47.47 /
42.92），三个种子里一个降、两个抬升不到 10 个决策步；`completed` 0/603。**按预登记
判据，这是"平"，停是对的。** 回报改善 +2.05 / +2.63 / +3.67 不构成继续的理由，你们
自己在 §5 里也是这么写的——这一点判断是对的，我不推翻。

同时认可你们的四条"不能确认"，尤其第 1 条：**这批结果不能判 `radial_local` 不可学**，
只能判"当前完整配置在 ~10k 步未过门槛"。任何后续文档若把这批写成"radial_local 失败"，
是超出证据的。三个 `interrupted_model.zip`、5k checkpoint、manifest、Monitor CSV
全部原样保留，这条照办。

---

## 2. 我方更正一：折扣因子（你们的发现）

我把本任务势函数整形写成 `gamma = 0.997`。**错。实际是
`PrecaptureRewardConfig.discount_factor = 0.999`**，代码和三份 manifest 都是 0.999。
`0.997` 属于 `Phase2MissionReward` 和 `train.configs.PURE_SAC`，是另一套任务的数。

已在 `docs/SAC_MPC_COUPLING_DESIGN.md` §9.3 改正，并把偏差从"估计"升级为**实测**：

| 量 | 值 | 怎么来的 |
|---|---:|---|
| 12 个种子 reset 时的势函数中位数 | **−87.4**（−103.5 ~ −73.4） | `PrecaptureReward.potential`，不涉及控制器 |
| 折扣泄漏 `(1−0.999)·|Φ|·0.05` | 0.0044 / 微步 | |
| 同上，每 2 s 决策 | 0.087 | |
| 同上，150 决策一整局 | **−13.1** | |
| 整个任务的望远镜信号（Φ 从 −87.4 到 ≈−4） | **+4.2** | |

**泄漏是它所依附信号的约 3 倍**，而且它只与"在远处待了多久"有关，与做了什么无关。
你们说"回报改善但 completion 为零与 reward/credit 不一致相容"——这条现在有量了。

---

## 3. 我方更正二：非法入口不锁死 episode（我自己复查发现）

我此前多处写过"非法穿越会永久锁死 episode"。**读代码后确认这是错的。**

`SE3RendezvousEnv._step` 中，穿越判定在 `self._terminal_region_entered` 仍为 false 的
每一步都执行；非法穿越只做 `self._illegal_terminal_entry_count += 1`，**不置任何标志、
不关闭闩锁**。退出圆盘后再次合法穿越，照常闩锁。

证据一致：`1fe5549` 上 262001 就是非法进入→退出→合法再进入→121.8 s 完成；今天 N-A
里 262003 同一 episode 内 `illegal=1`、`latched=1`、`completed=true`。

**真实成立的是行为层面那一半，而且它是对方法更有利的那一半：** 未闩锁地待在圆盘内时
`terminal_region_active` 为 false，终端约束不激活，完成判据永远过不了；而一个已经飞
到期望位姿的定点调节器没有任何理由再离开。于是它停在一个永远得不了分的区域里，因为
"退出去、等翻滚转过来、再敲一次门"不是它的代价函数能想要的动作。**门没锁，是下层
不会往回走。** 这是同一个缺口更干净的说法。

已更正 `docs/PRECAPTURE_ENTRY_WINDOW_GAP.md`（新增 §1b）与新文档。

**由此推出的一条否决：不要给非法入口加惩罚项。** 它不是规则性死亡，加惩罚就是在用外生
奖励把策略推向某种接近纪律，踩红线，而且会引入一个"惩罚多大"的可调数——那是最容易
被结果反推着调的旋钮。合法闩锁本身已经是通往 +20 的唯一门，稀疏但不是坏的。**非法入口
只记录、只诊断，不进 reward。**

---

## 4. 裁决：reward 时间尺度

**选宏观势函数一条**：在 2 s 决策边界上取一次势函数，用
`F = potential_weight · (γ_SAC · Φ(s_20) − Φ(s_0))`，`γ_SAC = 0.99`。

理由，不是偏好：

- 它在 SAC 真正优化的那一层**精确望远镜**，因此按构造是策略不变的。§2 的 −13.1 泄漏
  直接消失，不需要任何新参数。
- 你们提的另一条（严格按微步折扣聚合，宏观折扣取 `0.999²⁰ = 0.98019`）**忠实于错的
  东西**：`0.98019¹⁵⁰ = 0.0497`，等效视界约 50 个决策对 150 个决策的 episode——这正是
  `docs/SAC_MPC_COUPLING_DESIGN.md` §3 引入 2 s 粗步就是为了逃开的视界饥饿
  （Pure SAC 是 `0.997⁹⁵⁰ = 0.057`）。修完等于回到原点。

**改动范围严格限定：只有势函数项移到决策边界。** `time` / `force` / `torque` /
`safety` 四项继续按 20 个微步求和不动——它们是真正的逐步积分（燃料是冲量、安全裕度是
逐步暴露），把积分按块求和正是积分该有的行为，移走会改变它们度量的东西。

这一条作为**单因子**，独立 commit + manifest。不要同时扫 γ、ent_coef、网络、动作增益。

---

## 5. ★ 比上面四条都靠前：N-A 结果说失效环节可能在 MPC 可行性

今天在上层沙箱把 N-A 跑完了（合并树 `fabdfe4`，含 F1；12 种子，定点参考，h20，
一格一 episode，四路并行所以**不取任何时间数字**）。

**完成数没动：8/12，和无 F1 时一样。除此之外全变了。**

| seed | 完成 | 结束 t (s) | 结束 r (m) | QP `infeasible` 步数 | 首次不可行 | 结束于 |
|---|---|---:|---:|---|---:|---|
| 262000 / 262002 / 262003 / 262004 | 是 | 113.5 / 115.8 / 154.5 / 126.5 | ≈3.2 | **0 / 1136 ~ 1546** | — | 完成 |
| 262007 / 262008 / 262009 / 262010 | 是 | 134.0 / 125.5 / 142.1 / 115.1 | ≈3.2 | **0** | — | 完成 |
| 262001 | 否 | 38.6 | 14.68 | **379 / 387** | 0.8 s, 16.75 m | FOV 违约 |
| 262006 | 否 | 34.9 | 17.22 | **283 / 350** | 6.7 s, 17.38 m | FOV 违约 |
| 262005 | 否 | 96.0 | 10.61 | **172 / 961** | 31.1 s, 11.98 m | FOV 违约 |
| 262011 | 否 | 103.2 | 30.00 | **760 / 1033** | 27.3 s, 14.37 m | 30 m 距离失败 |

**完成的每一局，每一步 QP 都可解；失败的每一局都进入不可行且再没完全恢复。零重叠。**

机理：`MPCController.command` 在求解失败时回退到**零力/零力矩**。在一般调节问题里这是
可接受的回退；在本任务不是——`Λ > 1`，15 m 处体固定目标点在 0.62 m/s 的圆周上，零推力
不是保持位置，是以 `ω·r` 被甩开，同时视线角速率按 1/r 涨。轨迹正是这样：力和力矩连续
几百步恰好为 0.0，FOV 单调爬到 50° 限（三次分别 49.92 / 49.92 / 49.83°），或距离一路
走到 30 m 边界。262001 最极端：**从第 8 步（0.8 s）就不可行，387 步里 379 步不可行，
它整局根本没有过控制器。**

树里有三个种子的 F1 前配对轨迹（1 s 采样步长，所以状态是 1 Hz 抽样）：

| seed | F1 前 | F1 后 |
|---|---|---|
| 262002 | 完成 115.8 s，采样 117/117 `optimal` | 完成 **115.8 s**，0/1159 不可行 |
| 262004 | 完成 126.5 s，采样 128/128 `optimal` | 完成 **126.5 s**，0/1266 不可行 |
| 262001 | **完成 121.8 s**，采样 123/123 `optimal` | **38.6 s 失败**，379/387 不可行 |

即：**F1 在本来就可行的种子上是无操作**（两个完成时间复现到记录精度 0.1 s），
**在 262001 上把一次 121.8 s 的完成变成了从头不可行**。F1 不是免费的：它用增加约束行
换入口前瞻，某些种子上这些行让问题从一开始就无解。

### 这对论文缺口的影响

**不是缩小，是改名，而且改到一个更有利的位置。** 无 F1 时四个失败是入口平面的决策失败；
有 F1 时是 12–17 m 处约束优化器的**可行性失败**，远在入口平面之前。定点参考是一个
"其约束问题在四个种子上从 12–17 m 起就无解"的参考。**选一个 MPC 真解得动的参考，
这正是一个决策，正是 `radial_local` 动作空间参数化的东西**，而且不依赖闩锁规则。
这就是 `南航` 那套"上层提议 / 下层滤成安全"的标准提问：上层该提议什么，才能让下层的
问题保持可解。

### 它对本轮训练失败的直接含义（尚未证实，必须测）

耦合环境里高层每 2 s 给一个航点，内层跑 20 个 MPC 步。**如果航点让 QP 不可行，这 20 步
就是 20 步零推力**，在 `Λ > 1` 下等于把飞行器扔出去。`hybrid_qp_zero_fallbacks` 这个字段
已经存在，但你们指出 Monitor 只存了终止那一行——所以现在**无法**判断 0/603 里有多少是
这条路径造成的。这正是下一步要测的东西，而且**不需要训练**。

---

## 6. 裁决：下一轮做什么，顺序

**D0（先做，无需训练，最便宜）——把失败归因测出来。**
用现有的三个 `interrupted_model.zip`（以及 `--control random` 作对照）串行、单进程跑
20 个 episode，开逐决策日志。要回答的就一件事：
**episode 是死在探索、死在 reward，还是死在 MPC 不可行 + 零推力回退？**
在 D0 出结果之前不要再开三种子长训练——本批已经花了 3×5.11 h，再盲开一轮同样量级。

**D1（单因子）——§4 的宏观势函数。** 独立 commit + manifest。

**D2（单因子，视 D0 结果决定是否提前到 D1 之前）——零推力回退。**
"MPC 解不动"和"MPC 解不动时放弃得很难看"是两件事，目前只有第二件被证实。候选替代：
沿用上一拍解的移位（warm-start shift），或放松终端行后重解。这是便宜的单因子问题。
**在测出来之前，不要写"MPC 在此工况下不可行"这种句子**，只能写"零推力回退在 `Λ > 1`
下是致命的"。

**D3——评估器兼容修正：同意提交，但单独一个 commit。**
那 4 个文件（`env/hybrid_env.py`、`experiments/evaluate_hybrid_policy.py`、
`experiments/evaluate_hybrid_scripted.py`、`tests/test_hybrid_env.py`）是评估基础设施，
不改训练前向语义，你们已跑到 `196 passed` 并做过 1 episode smoke。作为独立
"评估器支持 4D radial_local 参数化"提交，commit message 里写明**不改训练语义**。
你们那条"后续模型评估必须显式匹配训练的 4D 参数化，不能退回默认 3D absolute"是对的，
请在测试里钉一条，让退回默认 3D 时直接失败，而不是安静地评错。

---

## 7. 下一轮短门槛实验必须新增的诊断字段

你们 §7.2 要的清单，具体化如下。每个 episode 一条记录：

**终止归因（必须，现在完全缺）**
- `termination_reason`：完成 / 超时 / 30 m 距离失败 / 各外围速度违约 / FOV / keepout /
  终端区各约束——**逐项分开，不要合并成"失败"**
- `termination_time_s`、`termination_range_m`

**MPC 可行性（这轮的第一嫌疑）**
- 每个高层决策：`qp_status` 计数（`optimal` / `infeasible` / 其他）、
  `qp_zero_fallback_steps`（该 2 s 内 20 步里回退了几步）
- episode 级：`qp_infeasible_steps_total`、`first_infeasible_time_s`、
  `max_consecutive_zero_wrench_steps`
- **判据：如果失败 episode 的 `qp_infeasible_steps_total` 显著非零而完成 episode 为零，
  就是 N-A 里那同一个机理，问题不在 SAC。**

**入口门（只诊断，不进 reward）**
- `illegal_entry_times_s`（列表）、`legal_latch_time_s`（或 null）、
  `time_inside_disc_unlatched_s`

**动作与参考（判断探索是否在动）**
- 逐决策 `hybrid_waypoint_radius_m` 与当前半径之比、横向倾角 deg
- episode 级：命令半径的中位数与四分位、比值 <1 的比例（内收占比）

**执行器（用户的硬要求：推力力矩不能持续饱和）**
- `force_saturated_step_fraction`、`torque_saturated_step_fraction`、
  `max_consecutive_saturated_steps`

Monitor 的终止行不能替代上述任何一条——你们 §4 最后那段说得对，那是终止时刻的最后一个
高层决策信息，不是整条轨迹，不能用它的均值论证整局的可解性或半径分布。

---

## 8. 证据边界（不变，重申）

`precapture_planning_full_state_v1_24d` + `perception=null`：策略观测与 MPC 状态都来自
真值。可以声称的是 full-state 条件下的**调度/决策**结果；**不能**声称局部视觉、EKF、
估计误差或感知闭环。已写入 `docs/SAC_MPC_COUPLING_DESIGN.md` §9.1。

---

## 9. 本轮上层已提交

- `fabdfe4` docs：§9 三条边界（证据边界 / 缺失的单因子提交 / 折扣偏差）
- `5ad228e` measure：N-A 带 F1 的 12 种子扫描 + 证据 JSON + §9.3 折扣更正
- 本文件与 `docs/PRECAPTURE_ENTRY_WINDOW_GAP.md` §1b 更正

---

## 10. h50 列已跑完：缺口换了一个更强的说法

h50 与 h20 同 12 种子、同任务、同参考、同真值：

| horizon | 完成 | 配对平均时间 | 配对平均冲量 | 串行单进程实时 |
|---|---:|---:|---:|---|
| h20 (2 s) | 8/12 | 128.4 s | 208.2 N·s | **进得去**，p95 0.94×，300 步里 7 步超 |
| **h50 (5 s)** | **10/12** | **65.8 s** | **190.3 N·s** | **进不去**，p95 2.22×，**300 步全超** |
| h50 (F1 前) | 1/12 | — | — | — |

配对是指两个 horizon 都完成的那 8 个种子，所以时间和冲量可比。

**三件事同时成立：**

1. **h50 在每一个质量列上都赢** —— 多两个完成、时间少一半、冲量还少 8.6%。它不是拿燃料
   换速度。燃料是本工作的最终判据，所以这个排序算数。
2. **h50 跑不了。** 串行单进程、机器上没有别的负载、只计 `MPCController.command`
   （RK45 真值传播 6.95 ms/步不计入）：h50 平均 202.4 ms、p95 222.0 ms，**测的 300 步
   里 300 步都超 0.1 s 周期**。h20 平均 84.3 ms、p95 93.9 ms、只有 7/300 步超，两者的
   最大值都在第 0 步（CVXPY 建问题的冷启动），去掉后 h20 稳态最差 120.1 ms。
   绝对毫秒不跨机器可比，可报的是比值，且要注明实现（Python/CVXPY/CLARABEL/单核）与
   星载处理器慢一到两个数量级。
3. **F1 前那个"h50 比 h20 差"的结论被证实是在测 F1 的缺席**：1/12 → 10/12，
   `docs/PRECAPTURE_ENTRY_WINDOW_GAP.md` §3 当时拒绝把它读成 horizon 结论，是对的。

**于是缺口可以一句话说清：赢下每一个质量列的那个 horizon 跑不到实时，跑得到实时的那个
在三列上都更差。** 耦合控制器要打的目标就是 **h20 的算力、h50 的结果**，而且这两端现在
都是在本任务上实测的，不是从 `single_phase` 那几轮继承来的。这正是 `南航` Limitation 1
在本工况下的具体形态：长到足以规划接近的那个 horizon，其单步代价已经出了预算。

**对下层任务清单的影响：**

- **D0（失败归因）优先级不变，仍是第一件事。** 但增加一个必测项：耦合环境用的是 h20
  （`hybrid_mpc_config` 里 `horizon_steps=20`），所以 §5 那条不可行链路在耦合里是活的。
- **新增 D4（便宜，无需训练）：把 h50 当作耦合行的上界基准。** 论文表里应该有这一行——
  "非实时 h50" 作为性能上界、"实时 h20" 作为要打败的基线、耦合行居中或更好。
  这比"入口时刻一维扫描的 10/12 天花板"更硬，因为它不是手写规则的天花板，是一个真实
  控制器的性能，只是买不起。
- **D2（零推力回退）的收益现在也可估：** h20 的 4 个失败里 3 个是不可行级联，h50 的 2 个
  失败里 1 个是。回退若修好，h20 可能从 8/12 上到 11/12，那会**抬高基线、压缩耦合的裕度**
  —— 这是要提前知道的，不是坏事，是必须先测清楚再决定要不要修的东西。诚实的做法是先测，
  再决定论文用哪一版作基线，而不是因为它压缩裕度就不修。

h50 的两个失败是两种失败，不是一种：262006 是 §5 的中途抛弃（253/285 步不可行），
262005 不是（247/281 步可解，最长不可行连续 4 步）——它是**近距执行器饱和**：8.660 N
满推从 16.7 m 冲到 9.5 m 用 28 s，视线角速率按 1/r 涨，力矩打满 1.0392 N·m 仍压不住，
FOV 20 s 时 8.0° 到 28 s 时 49.5°。**长 horizon 买不到推力器没有的权限。**


---

## 11. D2 与 D3 都已跑完，都是负结果——但这是这轮最有价值的产出

裁决 §6 把 D2（零推力回退）列为待测单因子。它和一个更上游的因子 D3 现在都测完了，
**两个都是负结果**，而正是这两个负结果把缺口从"实现问题"钉成了"结构问题"。

### D2：改回退方式 —— 4/4 失败种子未获救

单因子 `infeasible_fallback`：`zero`（默认、历史行为）对 `shift`（继续飞上一拍可行解，
向前移一格）。12 种子、h20、定点参考，其余全同。

**完成数两边都是 8/12**，且八个从未遇到不可行的 episode **逐位复现**——完成时间到记录
精度 0.1 s、末端距离到 1e-12。这是因子干净的自检项，通过了。

| seed | `zero` | `shift` |
|---|---|---|
| 262001 | 38.6 s, FOV | **27.3 s, FOV** |
| 262005 | 96.0 s, FOV | **241.2 s, FOV** |
| 262006 | 34.9 s, FOV | 38.4 s, FOV |
| 262011 | 103.2 s, 30 m | **81.4 s, FOV** |

一个没救回来，而且两个方向都有：一个活到 2.5 倍，两个死得更早。

**轨迹证明回退按设计工作了，而假设仍然是错的。** 262001 上移位计划把相对角速率在 4.0 s
内从 0.01907 带到 0.00011 rad/s——被零回退抛弃的消旋机动**被完整做完**，飞行器正确共旋
——然后它比被抛弃时还早死 11 s。做完姿态计划没用，因为姿态不是约束项。`Λ > 1` 下没有推力
的飞行器不管姿态在干什么都被交给 `ω·r`，计划只改变它撑多久。

顺带一个不舒服但要写下来的推论：**什么都不做是意外保护性的。** 262001 上零推力活 38.6 s，
听优化器自己上一拍的建议活 27.3 s。

### D3：不可行来自松弛上限，拿掉它更糟

读约束装配才发现"不可行"是怎么来的。每条 precapture 约束都是**软的**：
`J x + s >= b`，`s >= 0`，二次惩罚 `constraint_slack_weight = 1e4`。但松弛本身有界，
`s <= constraint_slack_limit`，默认 **2.0**。**这个上限把软约束变回了硬约束**——归一化
裕度差过 −2.0 就不可行，惩罚定得再高也没用。

单因子 `constraint_slack_limit` 2.0 对 20.0，回退保持默认 `zero`：

| seed | 默认 2.0 | cap 20.0 | 实际用掉的松弛 |
|---|---|---|---:|
| 262000（对照，本来完成） | 113.5 s，0/1136 不可行 | 113.5 s，0/1136 | **0.000** |
| 262001 | 38.6 s，**379/387** | **7.8 s**，**0/79** | 4.602 |
| 262005 | 96.0 s，**172/961** | **48.4 s**，**0/485** | 2.201 |
| 262006 | 34.9 s，**283/350** | **14.2 s**，5/143 | 19.012 |

**上限确实是机理**：放开后不可行在两个种子上彻底消失、第三个几乎消失，优化器随即花掉的
松弛（4.6 / 2.2 / 19.0）正是 2.0 在拒绝的量。262005 只需要 2.201，刚过上限一点，就解释了
它全部 172 步不可行。

**而且它没有用**：三个失败种子全部死得更早。拿掉拒绝不会变出可行轨迹，只会变出一条明知
违约的解，真值那边违得更快——因为优化器现在朝违约开，而不是原地不动。

对照种子是论证的另一半：262000 在 1136 步里花掉的松弛**恰好是 0.000**，说明成功种子上
约束集是宽裕可满足的。（它的数字不是逐位一致：末端距离与 FOV 在第四位有效数字上有差，
因为改变量上界会改变 CLARABEL 的数值路径，1136 步闭环放大了它。结局与完成时间到 0.1 s
不变。）

### 合起来：退路被堵死了

实现层面的两条补救都已实测，都是负的——**求解器回退（4/4 未获救）、软化上限（3/3 更差）**。
不是悬而未决的调参问题。

于是关于四个 h20 失败，可以下的判断从"MPC 放弃得很难看"升级为：

> **在这些种子上，定点参考是一个在线性化 horizon 内不存在满足轨迹的参考。**
> 拒绝出解、回退怎么写、软化放多松，全是它的下游。**唯一还能改的是参考本身**——
> 而选参考正是上层要做的事。

这和 `docs/PRECAPTURE_ENTRY_WINDOW_GAP.md` §3 从另一侧的结论一致（下层没有"退出去、
绕一圈再来"的表征）。D2/D3 的作用是堵住那些能让它退化成实现缺陷的解释。

### 对下层任务清单的修订

- **D2 从"待做"改为"已完成，负结果"。** 不要再花时间改回退。
- **新增：不要动 `constraint_slack_limit`。** 已测，放开更糟；默认 2.0 保持不变。
- **D0（失败归因诊断）优先级不变，且现在更值得做**：耦合环境用的就是 h20，N-A 已证明
  h20 上失败与 QP 不可行完全重合。record 里加那三个数即可。
- **§9.3 §4 的 reward 单因子不变。**
- 我这边已加的两个开关（`infeasible_fallback`、`--slack-limit`）**默认都保持历史行为**，
  既有数字逐位可复现，不需要因为它们重测任何东西。
